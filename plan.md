1. **Research and Create Mission File**: As the KAIROS Orchestrator (Researcher agent), create a detailed mission file in `.agent-task/missions/` with the current timestamp. The file must include the required sections: Title, Problem Statement, Research Report, Design Doc (with database designs and sequence diagrams), Implementation Prompt, Priority, and Estimated Scope. The design must address the "Shared Task List" tracking dependencies in Cloud-Native (PostgreSQL) and Standalone (SQLite) modes, including the `FOR UPDATE SKIP LOCKED` and SQLite lock degradation strategies.
2. Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
