1. **Define Sandbox Policy and Result Type in `srcs/server/harness/bash_sandbox/sandbox.go`**: Create the `SandboxPolicy` struct with `AllowedHosts`, `ReadRestriction`, and `WriteRestriction` slices. Create the `Result` struct with `Stdout`, `Stderr`, `ExitCode`, and `Error` fields.
2. **Implement Pre-Execution Validation in `srcs/server/harness/bash_sandbox/sandbox.go`**: Write the `ValidateBashCommand(cmd string) error` function that strictly checks the command for Zsh expansions, process substitutions (`>()`, `<()`), old-style expansions (`$[]`), obfuscated variables, and known dangerous commands like `zmodload`.
3. **Implement Execution Function in `srcs/server/harness/bash_sandbox/sandbox.go`**: Implement the `RunSandboxed(ctx context.Context, cmd string, policy SandboxPolicy) (Result, error)` function. This function first calls `ValidateBashCommand`. If valid, it will execute the bash command using `exec.CommandContext`, ensuring basic directory isolation (e.g. running in a temp dir if WriteRestriction applies or if requested) as a rudimentary sandbox capability.
4. **Implement Telemetry**: In `srcs/server/telemetry/telemetry.go`, add counters for `ohc_bash_sandbox_violations_total` and a histogram for `ohc_bash_execution_duration_seconds`. Provide a `RecordBashSandboxViolation(ctx context.Context)` and `RecordBashExecutionDuration(ctx context.Context, duration float64)` methods to properly update telemetry. Use these methods in `ValidateBashCommand` (or caller) and `RunSandboxed`.
5. **Write Comprehensive Unit Tests in `srcs/server/harness/bash_sandbox/sandbox_test.go`**: Use `t.TempDir()` and `t.Setenv()` as specified. Include tests to achieve >90% coverage for `ValidateBashCommand` and `RunSandboxed`.
6. **Update GitHub Issue & Complete Pre-commit Steps**: Add findings and status updates via API, and complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
