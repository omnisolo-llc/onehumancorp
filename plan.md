1. **Schema Migration:** Create a new migration file `srcs/server/db/migrations/032_hybrid_rag_sync.sql` to modify `swarm_memory_embeddings` (adding `sync_status` and `last_sync_at` columns) with compatibility for both SQLite and PostgreSQL. I will make sure to use standard `ALTER TABLE ADD COLUMN` for each individual column for SQLite compatibility.
2. **Go Interfaces:** Create `srcs/server/hub/rag_sync.go` defining the `RAGSyncRecord` and `RAGSyncService` interface containing `FetchPendingSyncs`, `MarkSynced`, and `ProcessIncomingSync` as outlined in the mission file.
3. **Telemetry:** Add OpenTelemetry counters to `srcs/server/telemetry/telemetry.go` for `rag_records_synced_total` (in code: `RagRecordsSyncedTotal`) and `rag_sync_errors_total` (`RagSyncErrorsTotal`), including initialization logic and global recording functions.
4. **Concrete Implementation:** Provide a concrete implementation of `RAGSyncService` in `srcs/server/hub/rag_sync.go` that interacts with the `db.Provider` database wrapper.
5. **Testing:** Create `srcs/server/hub/rag_sync_test.go` to provide unit tests covering the synchronization flow, using the in-memory SQLite setup via `test.Setenv` or `sql.Open("sqlite", ":memory:")` and injecting it into the `db.NewSqliteProvider`.
6. **Integration / Verification:** Run `bazelisk test //srcs/server/... --test_output=errors` to ensure tests pass and there are no regressions.
7. **Pre-commit:** Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
