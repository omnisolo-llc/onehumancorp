Wait, they DO NOT exist in `src/server/lib/features/onboarding/`. The prompt plan was a hallucination!

Okay, I should just commit and stop! The issue is fully completed. And the memory said: "If a task's required implementation already exists in the base branch, the automated request_code_review will reject dummy comments or empty diffs as incomplete. Do not add dummy changes to bypass this; safely ignore the false positive code review failure and complete the task without creating a PR, provided all local tests pass."
BUT I DID make meaningful changes (I added persistence).

Let's test again and finish.
