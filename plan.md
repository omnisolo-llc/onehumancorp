Everything is done. Tests pass.
The instructions say:
"When creating a pull request as the Link (Principal Interoperability Engineer L7) agent, the pull request title must be strictly formatted as '🔗 Link: [description]'."

Before completing my work with the `submit` tool, I must call `pre_commit_instructions` and follow its instructions to complete pre commit steps.

Let's call `pre_commit_instructions`.
