1.  **Extract Leaked Claude Code**: Download `claude-code(2_1_88).tgz` from the URL to a temporary location `/tmp/claude-code` and extract it using `curl` and `tar`.
2.  **Audit the Environment Harness**: Search for "Harness", "sandbox", "bash", "execute", "tool" and "shell" within the `CC-Source/src` structure. Analyze how it restricts and executes operations, such as `tools/BashTool`, `SandboxManager`, `sandbox-adapter.ts`, `shouldUseSandbox`, `bashSecurity.ts`, `bashPermissions.ts`, etc.
3.  **Document Architecture**: Produce a Research Report document at `docs/research/agent-harness-architecture.md` outlining the environment isolation, telemetry, and execution layer of Claude Code, comparing it to OHC. Emphasize "SandboxManager" which limits filesystem/network access, validation steps for bash executions like `parseForSecurity` and AST-based parsers, and how telemetry wraps command outputs in `BashTool`. Include premium Mermaid.js charts, CSS token references (Glassmorphism, 20px blur, Outfit/Inter typography where applicable).
4.  **Create Mission Output**: Submit the research findings. Trigger the next step (Implementation) using the YAML block as specified in the exit condition.
5.  **Pre-commit Instructions**: Ensure proper testing, verification, review, and reflection are done.
6.  **Submit PR**: Create and submit a GitHub PR titled "🧹 Maintainer: [Research] Claude Code Agent Harness Deep Dive" incorporating the new research doc.
