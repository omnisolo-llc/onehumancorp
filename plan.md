1. **Explore the codebase and understand the tasks**: The task asks to apply the Next-Generation OHC Design System ("Premium Feel") to the codebase, involving `backdrop-filter: blur(20px) saturate(200%)`, `background: rgba(255, 255, 255, 0.03)`, `border: 1px solid rgba(255, 255, 255, 0.08)`, and typography (`Outfit`, `Inter`). The files modified so far are in `docs/` as they document these styling tokens.
2. **Apply tokens to frontend**: Now we need to search for the frontend code where these tokens might be missing or wrongly implemented. We know `srcs/app` is the flutter app representing the frontend. Since we couldn't find `blur` or `BackdropFilter` maybe we should look into `Container` with `BoxDecoration` and `Colors.white.withOpacity` or similar in the flutter files, and replace them with `BackdropFilter` and the new design system. Let's do a search for colors/opacity.
3. **Verify design implementation**: Provide verification.
4. **Pre-commit checks**: Run `pre_commit_instructions`.
