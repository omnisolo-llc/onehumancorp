1. **Integrate Router**: The API endpoints `ClaimHandler` and `AddHandler` must be integrated into `srcs/server/server.go` or `srcs/server/api/tasks/router.go` such that they are accessible by the frontend. Actually, I should check `srcs/server/server.go` to see where routers are registered.
2. **Integrate Frontend**: I need to use the `SharedTaskList` widget from `apps/desktop/lib/ui/shared_task_list.dart`. Looking at `srcs/app/lib/screens/orchestration/task_list_screen.dart`, there is already a `TaskListScreen` implemented! Wait, the instruction specifically said: "Create the UI task list widget in `apps/desktop/lib/ui/shared_task_list.dart`." and "You MUST use `ConsumerWidget` for state management...". So I need to modify `srcs/app/lib/screens/orchestration/task_list_screen.dart` to use the new widget from `apps/desktop/lib/ui/shared_task_list.dart`? No, the issue explicitly said `apps/desktop/lib/ui/shared_task_list.dart`, but the frontend code is in `srcs/app/`. There is no `apps/desktop`! Wait, there IS an `apps/desktop` folder! Let's check `ls -la apps/desktop/`.
3. **E2E Test**: The E2E test `tests/e2e/tasks_e2e.spec.ts` needs to navigate to `/orchestration/tasks` which is the actual route in `srcs/app/lib/router.dart`.
4. Run tests and review again.
