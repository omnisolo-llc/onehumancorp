1. **Mark Mission In Progress:** Update `.agent-task/missions/2026-04-01T22-00-01Z.md` with `status: IN_PROGRESS` and `agent: Guide` by executing `sed -i 's/status: PENDING/status: IN_PROGRESS/' .agent-task/missions/2026-04-01T22-00-01Z.md` and `sed -i 's/agent: Researcher/agent: Guide/' .agent-task/missions/2026-04-01T22-00-01Z.md` via `run_in_bash_session`. Then verify the modification using `cat`.
2. **Interface Definition:** Read and verify `srcs/server/orchestration/mesh/mesh.go` defining the `TeammateMesh` interface, `Subscription`, and `AgentPresence` using `read_file`.
3. **Redis Provider:** Create `srcs/server/orchestration/mesh/redis_mesh.go` implementing the interface using `github.com/redis/go-redis/v9`. Implement Distributed Locking using SET NX PX with lua script validation for ReleaseLock using `write_file`. Verify the file creation and contents using `read_file`.
4. **Local Provider:** Create `srcs/server/orchestration/mesh/local_mesh.go` for Standalone mode, using in-memory channels, mutexes, and standard maps using `write_file`. Verify the file creation and contents using `read_file`.
5. **Testing:** Create `srcs/server/orchestration/mesh/mesh_test.go` with unit tests for `Publish`, `Subscribe`, `AcquireLock`, and `ReleaseLock` in `local_mesh` using `write_file`. Verify the file creation and contents using `read_file`.
6. **Main Injection Verification:** Use `run_in_bash_session` to run `grep -C 5 "orchestration.NewRedisMeshTransport" srcs/server/main.go` to verify the anchor string formatting in `main.go`.
7. **Main Injection Edit:** Use `run_in_bash_session` to execute `sed -i '/var mesh orchestration.MeshTransport/i \\tvar teammateMesh mesh.TeammateMesh\n\tif redisURL := os.Getenv("REDIS_URL"); redisURL != "" \&\& os.Getenv("OHC_STANDALONE") != "true" {\n\t\tteammateMesh = mesh.NewRedisTeammateMesh(redisURL)\n\t} else {\n\t\tteammateMesh = mesh.NewLocalTeammateMesh()\n\t}\n' srcs/server/main.go` to inject the `mesh.TeammateMesh` initialization logic into `srcs/server/main.go` near the existing `orchestration.MeshTransport` initialization. Then verify the edit using `cat`. (Note: I will actually write a script to insert this properly to avoid sed escaping hell). Let me write a python script for this step. Actually I'll use `replace_with_git_merge_diff` instead of `sed`.
8. **Run Tests:** Execute `bazelisk test //srcs/server/...` via `run_in_bash_session` to ensure all tests pass.
9. Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
10. **Submit:** Modify `.agent-task/missions/2026-04-01T22-00-01Z.md` to set `status: DONE` using `sed -i 's/status: IN_PROGRESS/status: DONE/' .agent-task/missions/2026-04-01T22-00-01Z.md` in `run_in_bash_session`. Then create a PR titled "🗺️ Guide: [Teammate Mesh API implementation]" using the `submit` tool.
