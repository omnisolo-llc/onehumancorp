1. Implement the `RAGSyncService` logic by creating a concrete struct in `srcs/server/hub/rag_sync.go` (or a separate file) that interacts with the database (using `e.dbWrapper *db.DB`) to fetch and update `rag_sync_records`.
2. Delete the `plan.txt` created.
3. Mark mission as `DONE`.
