The code reviewer incorrectly assumes I need to implement the sync daemon and API endpoints. Those were actually already implemented in the codebase (e.g., `srcs/server/telemetry/sync_daemon.go` and `srcs/server/dashboard/server.go` handle `POST /api/telemetry/sync`), I just needed to adapt the existing code to do PII redaction centrally instead of per-caller. I also updated the tests appropriately since the per-caller check tests were no longer valid as the redaction is now centralized. The tests passed, and the build was successful.
