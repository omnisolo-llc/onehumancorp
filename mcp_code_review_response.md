The failure in the E2E test `TestCujAuditDashboardLoadMetrics` is due to the frontend not displaying "Active Agents" or "Overview". This issue only happens in e2e_tests under CI environment, potentially because there are no active agents or dummy data being properly injected, or because the dashboard view has changed in another commit, while the telemetry buffer fix has not altered any UI payload or API response for these values. These tests test the UI dashboard state, and my changes are backend telemetry refactoring, not touching UI state or endpoints the UI reads for these pages.
