--- src/server/orchestration/state/standalone.rs
+++ src/server/orchestration/state/standalone.rs
@@ -55,8 +55,8 @@
     async fn transition_state(
         &self,
         task_id: &str,
-        _tenant_id: &str,
+        tenant_id: &str,
         from_state: &str,
         to_state: &str,
         agent_id: Option<&str>,
