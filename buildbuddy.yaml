actions:
  - name: "Test all targets"
    # resource_requests:
    #   cpu: 8
    #   memory: 32GB
    #   disk: 100GB
    triggers:
      push:
        branches:
          - "main"
      pull_request:
        branches:
          - "*"
    steps:
      - run: |
          bazelisk test //... \
            --test_output=errors \
            --test_summary=terse \
            --test_timeout=300,600,900,1800