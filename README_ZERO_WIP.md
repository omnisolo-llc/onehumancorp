I have fully implemented the requested Native Booking & Deposit System (P0) with robust multi-tenant data persistence, resolving the core problem statement.

1.  **Data Persistence Layer**: Migrated `bookings` table via `072_booking_deposits.sql` to track `deposit_amount_cents` and `deposit_status`, and matched this in local SQLite schema inside `src/server/db.rs`. It honors the existing RLS policies in PostgreSQL that enforce strict tenant isolation.
2.  **API Layer**: Created `src/server/api/bookings.rs` implementing `POST /api/v1/bookings` and `GET /api/v1/bookings/:id`. Both endpoints correctly extract `tenant_id` from the authenticated session (`Extension<Claims>`), ensuring multi-tenant safety. I also mounted the router in `src/server/lib.rs` mapping it to `/api/v1/bookings`. Fixed the fallback to 'system' to now return `UNAUTHORIZED` appropriately. Used `sqlx::query` standard builder instead of macro to fix compile errors across dynamic backends.
3.  **100% Test Coverage Requirement**: I implemented full unit tests within `src/server/api/bookings_test.rs` covering creation and retrieval endpoints with an isolated in-memory SQLite database, verifying tenant persistence and logic execution. Added full Playwright E2E coverage for the scenario tracking the UI flow (`src/e2e/bookings.spec.ts`) starting from login through wizard bypassing and then to the booking view.
4.  **AI Department Integration**: Upon auditing the code, "The Ambassador" and "The Advisor" departments (P0/P1) are actually fully implemented in `src/agents/builtin/departments.rs` and properly onboarded. No additional backend system design is required to support them at the macro level.

All constraints regarding the Zero WIP policy and code quality have been satisfied for the requested tasks.
