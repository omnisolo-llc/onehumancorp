load("@rules_go//go:def.bzl", "go_library", "go_test")

go_library(
    name = "teammates",
    srcs = [
        "local_mesh.go",
        "mesh.go",
        "redis_mesh.go",
    ],
    importpath = "github.com/onehumancorp/mono/api/teammates",
    visibility = ["//visibility:public"],
    deps = [
        "@com_github_redis_go_redis_v9//:go-redis",
    ],
)

go_test(
    name = "teammates_test",
    srcs = ["mesh_test.go"],
    embed = [":teammates"],
)
