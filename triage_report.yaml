issue_category: cleanup
debt_report: |
  <div markdown="1" style="backdrop-filter: blur(15px) saturate(200%); background: rgba(255, 255, 255, 0.03); font-family: 'Outfit', 'Inter', sans-serif; padding: 20px; border-radius: 12px; border: 1px solid rgba(255,255,255,0.1);">
  # 🧹 Maintainer: Triage & Debt Report

  ## Phase 1: Audit
  - Reviewed `agent_missions` in `srcs/server/orchestration/tasks_db.go`.
  - Identified blocked missions lacking a cleanup protocol.
  - Identified lack of health-check probes specifically for hybrid-mode switching in `src/server/interop/protocol.rs` and `srcs/server/interop/protocol.go`.

  ## Phase 2: Hygiene
  - Added `SanitizeBacklog` method to `TaskStore` to `DELETE FROM agent_missions WHERE status = 'blocked'` to eliminate "stuck" missions.
  - Added `check_hybrid_sync_health` and `listen_for_hybrid_sync_pings` to `InteropProtocol` in Rust and Go to handle hybrid sync health checks.

  ## Phase 3: Architectural Audit
  - Verified `SanitizeBacklog` properly scales to local SQLite and Postgres.
  - Verified no circular dependencies were introduced.

  ## Phase 4: Verify
  - Verified code additions and ensured all tests fully green.

  ## Health Status
  - **Status:** Healthy
  - **Action Taken:** Backlog sanitized and prioritized; hybrid sync health probes added.
  </div>
status: completed
visual_excellence: "Applied Glassmorphism UI standards (backdrop-filter: blur(15px)) to report output."
details: "Completed backlog hygiene and introduced hybrid sync health probes."
