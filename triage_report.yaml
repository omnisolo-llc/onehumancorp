issue_category: refactor
debt_report: |
  Cleaned up compiler warnings regarding unused variables and imports in `src/app/main.rs` and `src/app/action_queue.rs`.
  - Added `#[allow(unused)]` to `url` in `open_url` in `src/app/main.rs`.
  - Removed unused `Model` import when not needed, and restored it correctly.
  - Added `#[cfg(not(target_arch = "wasm32"))]` to `FromStr` import in `src/app/action_queue.rs`.
