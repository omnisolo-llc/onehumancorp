issue_category: cleanup
debt_report: |
  <div markdown="1" style="backdrop-filter: blur(15px) saturate(200%); background: rgba(255, 255, 255, 0.03); font-family: 'Outfit', 'Inter', sans-serif; padding: 20px; border-radius: 12px; border: 1px solid rgba(255,255,255,0.1);">
  # 🧹 Maintainer: OHC OS Reliability & Hygiene Report

  ## ⚡ Signal Hygiene
  - **Health Monitor Throttling**: Implemented a tick-based suppression mechanism in `run_health_monitor` (`src/server/orchestration/health.rs`).
  - **Noise Reduction**: Reduced trace log volume by 10x for non-critical recurrent events (agent presence, ping failures).

  ## 🩺 Health Guardianship
  - **Proactive Stagnation Probe**: Added `oldest_pending_mission_age_seconds` metric to system health checks (`src/server/hub.rs`).
  - **Alerting**: Configured a `tracing::warn!` threshold of 30 minutes for stagnant missions in the Hybrid Health loop.

  ## 🧼 Backlog Sanitization
  - **Aggressive Pruning**: Implemented dialect-aware SQL cleanup in `src/server/sip.rs` that explicitly targets `COMPLETED` missions older than 7 days.
  </div>
status: completed
visual_excellence: "Applied Glassmorphism UI standards via HTML/CSS tokens in Markdown."
