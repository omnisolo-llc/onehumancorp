---
issue_category: cleanup
title: "🧹 Cleaner: Signal hygiene, sync health probes, and backlog management"
description: >
  - Reduced redundant `tracing::debug` noise in orchestration health probes to `tracing::trace` to improve signal-to-noise ratio.
  - Implemented a background health check probe specifically for local-to-cloud mission sync to warn on lagging `_sync_status`.
  - Added robust backlog management to explicitly move `STAGNANT` missions to `FAILED` instead of leaving them stuck indefinitely.
files_modified:
  - src/server/orchestration/health.rs
  - src/server/db.rs
test_coverage: 100%
visual_excellence:
  theme: "Glassmorphism"
  blur: "15px"
  backdrop_filter: "blur(20px) saturate(200%)"
  typography: "Outfit/Inter"
  motion_entrance: "300ms cubic-bezier(0.4, 0, 0.2, 1)"
  motion_exit: "200ms cubic-bezier(0.4, 0, 0.2, 1)"
