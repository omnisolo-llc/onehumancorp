issue_category: cleanup
status: resolved
details:
  - Pruned stale missions logic implemented in sync_daemon to correctly manage stuck tasks in the agent_missions queue via SQLite sub_agent_jobs dependency resolution.
  - Implemented health check adjustments for hybrid-mode timeouts in src/server/orchestration/health.rs.
  - Fixed flaky timeouts in src/server:server_test by correctly specifying size="large" in BUILD.bazel to accommodate for test execution duration.
  - Verified codebase health and ensuring all tests pass successfully with bazelisk test //...
debt_report: |
  Cleaned up circular references and stale sync locks causing infinite scaling.
