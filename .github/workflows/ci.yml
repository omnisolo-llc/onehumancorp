name: CI

on:
  workflow_call:
    inputs:
      performance_prefix:
        description: Exact reusable-workflow job name prefix for timing evidence
        type: string
        default: ''
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "23 10 * * *"
  workflow_dispatch:
    inputs:
      cold_cache:
        description: Disable compiler, Node-download and Next caches on fresh runners
        type: boolean
        default: false

concurrency:
  group: native-ci-${{ github.workflow }}-${{ github.event_name == 'pull_request' && format('/pr/{0}', github.event.pull_request.number) || github.event_name == 'schedule' && 'schedule' || github.event_name == 'workflow_dispatch' && format('manual/{0}', github.run_id) || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' || github.event_name == 'schedule' }}

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

permissions:
  contents: read

defaults:
  run:
    shell: bash

jobs:
  check-changes:
    name: Check what files changed
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      markdown-only: ${{ steps.check.outputs.markdown-only }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Validate migration identities before starting expensive builds
        run: bash src/server/migrations/sqlx_migration_contract_test.sh

      - name: Reject active Chatwoot residue
        run: bash deploy/tests/no_chatwoot_residue_test.sh

      - name: Check if only markdown files changed
        id: check
        env:
          CI_PERFORMANCE_PREFIX: ${{ inputs.performance_prefix || '' }}
        run: |
          if [[ -n "$CI_PERFORMANCE_PREFIX" || "${{ github.ref_type }}" == tag ]]; then
            echo "markdown-only=false" >> "$GITHUB_OUTPUT"
            exit 0
          elif [[ "${{ github.event_name }}" == "pull_request" ]]; then
            git fetch origin "${{ github.base_ref }}"
            changed_files=$(git diff --name-only "origin/${{ github.base_ref }}...HEAD")
          elif [[ "${{ github.event_name }}" == "push" ]]; then
            before="${{ github.event.before }}"
            if [[ "$before" == 0000000000000000000000000000000000000000 ]] || ! git cat-file -e "$before^{commit}" 2>/dev/null; then
              echo "markdown-only=false" >> "$GITHUB_OUTPUT"
              exit 0
            fi
            changed_files=$(git diff --name-only "$before" "${{ github.sha }}")
          else
            echo "markdown-only=false" >> "$GITHUB_OUTPUT"
            echo "Full run; treating as non-markdown change set"
            exit 0
          fi

          echo "Changed files:"
          echo "$changed_files"

          non_md_files=$(echo "$changed_files" | grep -v '\.md$' | grep -v '^$' || true)

          if [ -z "$non_md_files" ]; then
            echo "markdown-only=true" >> "$GITHUB_OUTPUT"
            echo "Only markdown files changed"
          else
            echo "markdown-only=false" >> "$GITHUB_OUTPUT"
            echo "Non-markdown files changed: $non_md_files"
          fi

      - name: Install PyYAML
        run: |
          sudo apt-get update && sudo apt-get install -y python3-yaml

      - name: Check native release identity and workflow contracts
        run: |
          python3 scripts/release_contract_test.py
          python3 scripts/release_pipeline_test.py
          node --test scripts/release-workflow.test.mjs

      - name: Check developer bootstrap and required-job contracts
        run: |
          python3 .github/scripts/check_checkout_paths_test.py
          python3 scripts/init_dev_test.py
          python3 .github/scripts/check_native_cache_test.py

      - name: Check tracked artifacts
        run: |
          .github/scripts/check_repo_hygiene_test.sh
          .github/scripts/check_repo_hygiene.sh
          python3 .github/scripts/check_postgres_security_ci_test.py
          python3 .github/scripts/check_postgres_security_ci.py

  dependency-audit:
    name: Production dependency audit
    needs:
      - check-changes
    if: ${{ needs.check-changes.outputs.markdown-only == 'false' }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version-file: .node-version
          package-manager-cache: false

      # npm lockfiles are authoritative for the native Node build. Do not audit
      # a second, stale package-manager resolution that the product never uses.

      - name: Audit root npm lock
        run: npm audit --omit=dev

      - name: Audit UI npm lock
        working-directory: src/ui/next
        run: npm audit --omit=dev

  native-build:
    name: Native backend binaries
    needs: [check-changes]
    if: ${{ needs.check-changes.outputs.markdown-only == 'false' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v6
      - name: Install native system libraries
        run: sudo apt-get update && sudo apt-get install -y pkg-config libssl-dev
      - uses: ./.github/actions/setup-native
        with:
          role: backend-debug
          node: 'false'
          cache: ${{ github.event.inputs.cold_cache != 'true' }}
      - name: Build Cargo binaries
        run: cargo build --locked -p omnisolo -p omnisolo_builtin_agent -p omnisolo_harness_worker --bins --timings
      - name: Preserve actual compiler timing evidence
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: native-backend-compiler-timings
          path: target/cargo-timings/*.html
          if-no-files-found: warn
          retention-days: 7
      - uses: actions/upload-artifact@v6
        with:
          name: native-linux-binaries
          path: |
            target/debug/server
            target/debug/grpc-mtls-probe
            target/debug/omnisolo-builtin-agent
            target/debug/omnisolo-harness-worker
          if-no-files-found: error
          retention-days: 3

  native-test:
    name: Native Rust tests
    env:
      # Keep memory-heavy test compilation separate; preserve the security
      # lane's independent, deliberately minimal environment contract.
      CARGO_BUILD_JOBS: "1"
      RUST_TEST_THREADS: "1"
    needs: [check-changes]
    if: ${{ needs.check-changes.outputs.markdown-only == 'false' }}
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v6
      - name: Install native system libraries
        run: sudo apt-get update && sudo apt-get install -y pkg-config libssl-dev
      - uses: ./.github/actions/setup-native
        with:
          role: backend-test
          node-scope: harness
          cache: ${{ github.event.inputs.cold_cache != 'true' }}
      - name: Verify pinned native harness test prerequisites
        run: |
          echo "$GITHUB_WORKSPACE/.github/test-tools/node_modules/.bin" >> "$GITHUB_PATH"
          version="$(.github/test-tools/node_modules/.bin/opencode --version)"
          test "$version" = '1.18.15'
      - name: Lint the complete headless Rust workspace before linking tests
        run: make lint-backend
      - name: Test complete headless Cargo workspace
        run: make test-backend

  native-node:
    name: Native Node quality gates
    needs: [check-changes]
    if: ${{ needs.check-changes.outputs.markdown-only == 'false' }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-native
        id: native-setup
        with:
          rust: 'false'
          node-scope: all
          cache: ${{ github.event.inputs.cold_cache != 'true' }}
      - name: Install Python workflow-contract dependency
        run: sudo apt-get update && sudo apt-get install -y python3-yaml
      # Independent checks retain their own failing conclusions. A lint failure
      # must not hide the entire unit suite, and no failed step is made optional.
      - name: Test preserved deployment and security contracts
        if: ${{ !cancelled() && steps.native-setup.outcome == 'success' }}
        run: make test-contracts
      - name: Lint JavaScript and TypeScript
        if: ${{ !cancelled() && steps.native-setup.outcome == 'success' }}
        run: npm run lint:node
      - name: Typecheck web and CLI
        if: ${{ !cancelled() && steps.native-setup.outcome == 'success' }}
        run: |
          npm run typecheck:web
          npm --prefix src/cli run typecheck
      - name: Run all Node, frontend, CLI and desktop UI tests
        if: ${{ !cancelled() && steps.native-setup.outcome == 'success' }}
        run: make test-node

  native-web:
    name: Native Next production build
    needs: [check-changes]
    if: ${{ needs.check-changes.outputs.markdown-only == 'false' }}
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-native
        with:
          rust: 'false'
          cache: ${{ github.event.inputs.cold_cache != 'true' }}
          node-scope: web
      - uses: actions/cache/restore@v5
        if: github.event.inputs.cold_cache != 'true'
        id: next-cache
        with:
          path: src/ui/next/.next/cache
          key: next-v2-${{ runner.os }}-${{ runner.arch }}-${{ env.ImageOS || 'custom-image' }}-${{ hashFiles('.node-version', 'package-lock.json', 'src/ui/next/package-lock.json', 'src/ui/next/next.config.mjs') }}-${{ github.sha }}
          restore-keys: next-v2-${{ runner.os }}-${{ runner.arch }}-${{ env.ImageOS || 'custom-image' }}-${{ hashFiles('.node-version', 'package-lock.json', 'src/ui/next/package-lock.json', 'src/ui/next/next.config.mjs') }}-
      # Publish this run's build promptly; independent quality gates remain
      # required by CI Required and cannot be bypassed by a successful build.
      - name: Build fresh Node server and assets
        run: make build-web
      - uses: actions/cache/save@v5
        if: github.event.inputs.cold_cache != 'true' && github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && steps.next-cache.outputs.cache-hit != 'true'
        with:
          path: src/ui/next/.next/cache
          key: ${{ steps.next-cache.outputs.cache-primary-key }}
      - uses: actions/upload-artifact@v6
        with:
          name: native-web
          path: target/native-web
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 3

  native-desktop:
    name: Native Tauri compile
    needs: [check-changes, native-web]
    if: ${{ needs.check-changes.outputs.markdown-only == 'false' }}
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v6
      - name: Install Tauri system libraries
        run: sudo apt-get update && sudo apt-get install -y libwebkit2gtk-4.1-dev build-essential libssl-dev libayatana-appindicator3-dev librsvg2-dev patchelf
      - uses: ./.github/actions/setup-native
        with:
          role: desktop-debug
          cache: ${{ github.event.inputs.cold_cache != 'true' }}
          node-scope: root
      - uses: actions/download-artifact@v7
        with:
          name: native-web
          path: target/native-web
      - name: Compile desktop with this run's Node build
        env:
          OMNISOLO_PREBUILT_WEB: target/native-web
        run: npm run desktop:build -- --debug --no-bundle
      - name: Lint desktop Rust (backend is checked in native-test)
        run: cargo clippy --locked -p app --all-targets -- -D warnings
      - name: Test desktop Rust
        run: cargo test --locked -p app

  native-e2e:
    name: Native real-stack Playwright ${{ matrix.shard }}/4
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix:
        shard: [1, 2, 3, 4]
    needs: [check-changes, native-build, native-web]
    if: ${{ needs.check-changes.outputs.markdown-only == 'false' }}
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-native
        with:
          rust: 'false'
          cache: ${{ github.event.inputs.cold_cache != 'true' }}
          node-scope: root
      - uses: actions/download-artifact@v7
        with:
          name: native-linux-binaries
          path: target/debug
      - uses: actions/download-artifact@v7
        with:
          name: native-web
          path: target/native-web
      - name: Install browser and native test inputs
        run: |
          chmod +x target/debug/server target/debug/omnisolo-builtin-agent target/debug/grpc-mtls-probe
          npx --no-install playwright install --with-deps chromium
      - name: Execute complete real-stack suite shard
        run: npm run test:e2e -- --ci --shard=${{ matrix.shard }}/4
      - uses: actions/upload-artifact@v6
        if: always()
        with:
          name: native-playwright-results-${{ matrix.shard }}
          path: |
            playwright-report
            test-results
          retention-days: 7

  native-images:
    name: Shared native production images
    needs: [check-changes]
    if: ${{ needs.check-changes.outputs.markdown-only == 'false' }}
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v6
      - name: Bind images to source before compilation
        id: image-source
        run: echo "digest=$(python3 scripts/native-images.py fingerprint)" >> "$GITHUB_OUTPUT"
      - uses: docker/setup-buildx-action@v4
      - name: Build actual production server image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: deploy/docker/server/Dockerfile
          target: server
          load: true
          push: false
          tags: omnisolo/server:latest
          no-cache: ${{ github.event.inputs.cold_cache == 'true' }}
          cache-from: ${{ github.event.inputs.cold_cache != 'true' && 'type=gha,scope=native-production-linux-amd64' || '' }}
          cache-to: ${{ github.event.inputs.cold_cache != 'true' && github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && 'type=gha,mode=max,scope=native-production-linux-amd64' || '' }}
      - name: Build agent using the same compiled production layers
        uses: docker/build-push-action@v7
        with:
          context: .
          file: deploy/docker/server/Dockerfile
          target: agent
          load: true
          push: false
          tags: omnisolo/agent:latest
          cache-from: ${{ github.event.inputs.cold_cache != 'true' && 'type=gha,scope=native-production-linux-amd64' || '' }}
      - name: Package verified images for both deployment suites
        env:
          EXPECTED_IMAGE_SOURCE: ${{ steps.image-source.outputs.digest }}
        run: python3 scripts/native-images.py package --directory target/ci-images --expected-source "$EXPECTED_IMAGE_SOURCE"
      - uses: actions/upload-artifact@v6
        with:
          name: native-production-images
          path: target/ci-images
          if-no-files-found: error
          retention-days: 3

  kind-e2e:
    name: Kind E2E
    needs:
      - check-changes
      - native-images
      - native-build
    if: ${{ needs.check-changes.outputs.markdown-only == 'false' }}
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      KIND_VERSION: "v0.31.0"
      KUBECTL_VERSION: "v1.36.1"
      HELM_VERSION: "v3.20.2"
      KIND_SHA256_AMD64: "eb244cbafcc157dff60cf68693c14c9a75c4e6e6fedaf9cd71c58117cb93e3fa"
      KIND_SHA256_ARM64: "8e1014e87c34901cc422a1445866835d1e666f2a61301c27e722bdeab5a1f7e4"
      KUBECTL_SHA256_AMD64: "629d3f410e09bf49b64ae7079f7f0bda1191efed311f7d37fdbab0ad5b0ec2b7"
      KUBECTL_SHA256_ARM64: "59f7ee8e477fae658447607dc3c8790ac17a1b016c01c622c12070e969e2d4e7"
      HELM_SHA256_AMD64: "258e830a9e613c8a7a302d6059b4bb3b9758f2f3e1bb8ea0d707ce10a9a72fea"
      HELM_SHA256_ARM64: "5ea2d6bc2cda3f8edf985e028809f5a9278f404fb8ab24044de9b7cb9b79a691"
      MINIMAX_API_KEY: "ci-e2e-placeholder-not-a-real-key"
    steps:
      - uses: actions/checkout@v6

      - name: Free runner disk space
        run: |
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /opt/ghc
          sudo rm -rf "/usr/local/share/boost"
          sudo rm -rf "/usr/local/share/powershell"
          sudo rm -rf "/opt/hostedtoolcache/CodeQL"
          docker image prune --all --force
          sudo rm -rf /usr/lib/jvm
          sudo rm -rf /usr/local/graalvm
          sudo rm -rf /usr/local/julia
          sudo rm -rf /usr/local/rust

      - name: Install Kubernetes tools
        run: |
          case "$(uname -m)" in
            x86_64)
              CI_ARCH=amd64
              KIND_SHA256="$KIND_SHA256_AMD64"
              KUBECTL_SHA256="$KUBECTL_SHA256_AMD64"
              HELM_SHA256="$HELM_SHA256_AMD64"
              ;;
            aarch64|arm64)
              CI_ARCH=arm64
              KIND_SHA256="$KIND_SHA256_ARM64"
              KUBECTL_SHA256="$KUBECTL_SHA256_ARM64"
              HELM_SHA256="$HELM_SHA256_ARM64"
              ;;
            *) echo "Unsupported CI architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          echo "CI_ARCH=${CI_ARCH}" >> "${GITHUB_ENV}"

          tool_dir="$(mktemp -d)"

          curl -fsSL -o "$tool_dir/kind" "https://kind.sigs.k8s.io/dl/${KIND_VERSION}/kind-linux-${CI_ARCH}"
          echo "${KIND_SHA256}  $tool_dir/kind" | sha256sum --check --strict
          chmod +x "$tool_dir/kind"
          sudo install "$tool_dir/kind" /usr/local/bin/kind

          curl -fsSL -o "$tool_dir/kubectl" "https://dl.k8s.io/release/${KUBECTL_VERSION}/bin/linux/${CI_ARCH}/kubectl"
          echo "${KUBECTL_SHA256}  $tool_dir/kubectl" | sha256sum --check --strict
          chmod +x "$tool_dir/kubectl"
          sudo install "$tool_dir/kubectl" /usr/local/bin/kubectl

          curl -fsSL -o "$tool_dir/helm.tar.gz" "https://get.helm.sh/helm-${HELM_VERSION}-linux-${CI_ARCH}.tar.gz"
          echo "${HELM_SHA256}  $tool_dir/helm.tar.gz" | sha256sum --check --strict
          tar -C "$tool_dir" -xzf "$tool_dir/helm.tar.gz"
          sudo install "$tool_dir/linux-${CI_ARCH}/helm" /usr/local/bin/helm

          kind version
          kubectl version --client=true
          helm version --short

      - name: Verify Docker is available
        run: |
          docker version
          docker info

      - uses: actions/download-artifact@v7
        with:
          name: native-production-images
          path: target/ci-images
      - uses: actions/download-artifact@v7
        with:
          name: native-linux-binaries
          path: target/debug
      - name: Use this run's already compiled mTLS probe
        run: chmod +x target/debug/grpc-mtls-probe
      - name: Restore Docker image cache
        id: kind-image-cache
        uses: actions/cache/restore@v5
        if: github.event.inputs.cold_cache != 'true'
        with:
          path: /tmp/omnisolo-kind-images
          key: ${{ runner.os }}-${{ runner.arch }}-kind-images-18d16372b840-94365b275456
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-kind-images-

      - name: Load and pull Docker images for Kind
        run: |
          if [[ -f /tmp/omnisolo-kind-images/images.tar ]]; then
            docker load -i /tmp/omnisolo-kind-images/images.tar
          fi
          docker pull pgvector/pgvector:pg15@sha256:18d16372b8406bb38a9f94cbff15d125c463d71fde2770aa8b5c64bfcc1578ee
          docker pull valkey/valkey:8-alpine@sha256:94365b275456ae14621001c03556c732b1d93a0cdeacc317d1bdd52eba680885
          mkdir -p /tmp/omnisolo-kind-images
          docker save \
            pgvector/pgvector:pg15@sha256:18d16372b8406bb38a9f94cbff15d125c463d71fde2770aa8b5c64bfcc1578ee \
            valkey/valkey:8-alpine@sha256:94365b275456ae14621001c03556c732b1d93a0cdeacc317d1bdd52eba680885 \
            -o /tmp/omnisolo-kind-images/images.tar

      - name: Execute native kind-e2e probes
        env:
          OMNISOLO_PREBUILT_IMAGES: target/ci-images
          OMNISOLO_GRPC_PROBE: target/debug/grpc-mtls-probe
        run: bash deploy/tests/kind_e2e_test.sh

      - name: Save verified base images from trusted main
        uses: actions/cache/save@v5
        if: github.event.inputs.cold_cache != 'true' && github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && steps.kind-image-cache.outputs.cache-hit != 'true'
        with:
          path: /tmp/omnisolo-kind-images
          key: ${{ steps.kind-image-cache.outputs.cache-primary-key }}

  docker-e2e:
    name: Docker E2E
    needs:
      - check-changes
      - native-images
      - native-build
    if: ${{ needs.check-changes.outputs.markdown-only == 'false' }}
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      MINIMAX_API_KEY: "ci-e2e-placeholder-not-a-real-key"
    steps:
      - uses: actions/checkout@v6

      - name: Free runner disk space
        run: |
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /opt/ghc
          sudo rm -rf "/usr/local/share/boost"
          sudo rm -rf "/usr/local/share/powershell"
          sudo rm -rf "/opt/hostedtoolcache/CodeQL"
          docker image prune --all --force
          sudo rm -rf /usr/lib/jvm
          sudo rm -rf /usr/local/graalvm
          sudo rm -rf /usr/local/julia
          sudo rm -rf /usr/local/rust

      - name: Verify Docker is available
        run: |
          docker version
          docker info

      - name: Restore Docker Compose image cache
        id: compose-image-cache
        uses: actions/cache/restore@v5
        if: github.event.inputs.cold_cache != 'true'
        with:
          path: /tmp/omnisolo-docker-compose-images
          key: ${{ runner.os }}-${{ runner.arch }}-docker-compose-images-${{ hashFiles('deploy/docker-compose.yml', 'deploy/docker-compose.override.yml') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-docker-compose-images-

      - name: Load Docker Compose image cache
        run: |
          if [[ -f /tmp/omnisolo-docker-compose-images/images.tar ]]; then
            docker load -i /tmp/omnisolo-docker-compose-images/images.tar
          fi

      - name: Pull Docker Compose images
        run: |
          # Keep these identical to the effective base + E2E override stack.
          docker pull pgvector/pgvector:pg15@sha256:18d16372b8406bb38a9f94cbff15d125c463d71fde2770aa8b5c64bfcc1578ee
          docker pull valkey/valkey:8-alpine@sha256:94365b275456ae14621001c03556c732b1d93a0cdeacc317d1bdd52eba680885
          docker pull curlimages/curl:8.10.1@sha256:d9b4541e214bcd85196d6e92e2753ac6d0ea699f0af5741f8c6cccbfcf00ef4b
          mkdir -p /tmp/omnisolo-docker-compose-images
          docker save \
            pgvector/pgvector:pg15@sha256:18d16372b8406bb38a9f94cbff15d125c463d71fde2770aa8b5c64bfcc1578ee \
            valkey/valkey:8-alpine@sha256:94365b275456ae14621001c03556c732b1d93a0cdeacc317d1bdd52eba680885 \
            curlimages/curl:8.10.1@sha256:d9b4541e214bcd85196d6e92e2753ac6d0ea699f0af5741f8c6cccbfcf00ef4b \
            -o /tmp/omnisolo-docker-compose-images/images.tar

      - uses: actions/download-artifact@v7
        with:
          name: native-production-images
          path: target/ci-images
      - uses: actions/download-artifact@v7
        with:
          name: native-linux-binaries
          path: target/debug
      - name: Use this run's already compiled mTLS probe
        run: chmod +x target/debug/grpc-mtls-probe
      - name: Execute native docker-e2e probes
        env:
          OMNISOLO_PREBUILT_IMAGES: target/ci-images
          OMNISOLO_GRPC_PROBE: target/debug/grpc-mtls-probe
        run: bash deploy/tests/docker_compose_e2e_test.sh

      - name: Save verified base images from trusted main
        uses: actions/cache/save@v5
        if: github.event.inputs.cold_cache != 'true' && github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && steps.compose-image-cache.outputs.cache-hit != 'true'
        with:
          path: /tmp/omnisolo-docker-compose-images
          key: ${{ steps.compose-image-cache.outputs.cache-primary-key }}

  postgres-security:
    name: PostgreSQL tenant isolation
    needs:
      - check-changes
    if: ${{ needs.check-changes.outputs.markdown-only == 'false' }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: ohc_security
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d ohc_security"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 12
    env:
      OMNISOLO_REQUIRE_POSTGRES_TESTS: "1"
      OMNISOLO_POSTGRES_ADMIN_URL: postgresql://postgres:postgres@127.0.0.1:5432/ohc_security
      OMNISOLO_DATABASE_URL: postgresql://ohc_security_test:ohc_security_test@127.0.0.1:5432/ohc_security
    steps:
      - uses: actions/checkout@v6

      - uses: ./.github/actions/setup-native
        with:
          role: postgres-security
          node: 'false'
          cache: ${{ github.event.inputs.cold_cache != 'true' }}
      - name: Install PostgreSQL client
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends postgresql-client protobuf-compiler

      - name: Provision and verify non-superuser application role
        run: |
          psql "$OMNISOLO_POSTGRES_ADMIN_URL" --set ON_ERROR_STOP=1 <<'SQL'
          DO $$
          BEGIN
            IF NOT EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'ohc_bypassrls') THEN
              CREATE ROLE ohc_bypassrls;
            END IF;
            IF NOT EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'ohc_security_test') THEN
              CREATE ROLE ohc_security_test LOGIN PASSWORD 'ohc_security_test';
            END IF;
          END
          $$;
          ALTER ROLE ohc_bypassrls BYPASSRLS NOLOGIN;
          ALTER ROLE ohc_security_test NOSUPERUSER NOCREATEDB NOCREATEROLE NOINHERIT NOBYPASSRLS;
          GRANT ohc_bypassrls TO ohc_security_test;
          GRANT CONNECT ON DATABASE ohc_security TO ohc_security_test;
          SQL

          psql "$OMNISOLO_DATABASE_URL" --set ON_ERROR_STOP=1 <<'SQL'
          DO $$
          BEGIN
            IF NOT EXISTS (
              SELECT 1
              FROM pg_roles
              WHERE rolname = current_user
                AND current_user = 'ohc_security_test'
                AND NOT rolsuper
                AND NOT rolinherit
                AND NOT rolbypassrls
                AND pg_has_role(current_user, 'ohc_bypassrls', 'MEMBER')
            ) THEN
              RAISE EXCEPTION 'PostgreSQL security lane is not using the NOBYPASSRLS application role';
            END IF;
            IF NOT (current_setting('row_security') = 'on') THEN
              RAISE EXCEPTION 'row_security must be on';
            END IF;
          END
          $$;
          SELECT current_user, rolsuper, rolinherit, rolbypassrls,
                 pg_has_role(current_user, 'ohc_bypassrls', 'MEMBER') AS bypass_member,
                 current_setting('row_security')
          FROM pg_roles
          WHERE rolname = current_user;
          SQL

      - name: Run PostgreSQL tenant-isolation suite
        run: "cargo test --locked -p server_auth multitenancy_isolation:: -- --nocapture"

  ci-required:
    name: CI Required
    needs:
      - check-changes
      - dependency-audit
      - native-build
      - native-test
      - native-e2e
      - native-web
      - native-node
      - native-images
      - native-desktop
      - kind-e2e
      - docker-e2e
      - postgres-security
    if: ${{ always() }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
      actions: read
    steps:
      - name: Check required CI results
        env:
          EVENT_NAME: ${{ github.event_name }}
          MARKDOWN_ONLY: ${{ needs.check-changes.outputs.markdown-only }}
          DEPENDENCY_AUDIT_RESULT: ${{ needs.dependency-audit.result }}
          CHECK_CHANGES_RESULT: ${{ needs.check-changes.result }}
          NATIVE_BUILD_RESULT: ${{ needs.native-build.result }}
          NATIVE_TEST_RESULT: ${{ needs.native-test.result }}
          NATIVE_E2E_RESULT: ${{ needs.native-e2e.result }}
          NATIVE_WEB_RESULT: ${{ needs.native-web.result }}
          NATIVE_NODE_RESULT: ${{ needs.native-node.result }}
          NATIVE_IMAGES_RESULT: ${{ needs.native-images.result }}
          NATIVE_DESKTOP_RESULT: ${{ needs.native-desktop.result }}
          KIND_E2E_RESULT: ${{ needs.kind-e2e.result }}
          DOCKER_E2E_RESULT: ${{ needs.docker-e2e.result }}
          POSTGRES_SECURITY_RESULT: ${{ needs.postgres-security.result }}
        run: |
          set -euo pipefail

          echo "dependency-audit: ${DEPENDENCY_AUDIT_RESULT}"
          echo "check-changes: ${CHECK_CHANGES_RESULT}"
          echo "native-build: ${NATIVE_BUILD_RESULT}"
          echo "native-test: ${NATIVE_TEST_RESULT}"
          echo "native-e2e: ${NATIVE_E2E_RESULT}"
          echo "native-web: ${NATIVE_WEB_RESULT}"
          echo "native-node: ${NATIVE_NODE_RESULT}"
          echo "native-images: ${NATIVE_IMAGES_RESULT}"
          echo "native-desktop: ${NATIVE_DESKTOP_RESULT}"
          echo "kind-e2e: ${KIND_E2E_RESULT}"
          echo "docker-e2e: ${DOCKER_E2E_RESULT}"
          echo "postgres-security: ${POSTGRES_SECURITY_RESULT}"

          require_success() {
            local name="$1"
            local result="$2"
            if [[ "$result" != "success" ]]; then
              echo "::error::${name} finished with result '${result}', expected 'success'."
              false
            fi
          }

          allow_success_or_skipped() {
            local name="$1"
            local result="$2"
            if [[ "$result" != "success" && "$result" != "skipped" ]]; then
              echo "::error::${name} finished with result '${result}', expected 'success' or 'skipped'."
              false
            fi
          }

          require_success "check-changes" "$CHECK_CHANGES_RESULT"

          if [[ "$MARKDOWN_ONLY" != "true" || "$EVENT_NAME" == "schedule" || "$EVENT_NAME" == "workflow_dispatch" ]]; then
            require_success "native-build" "$NATIVE_BUILD_RESULT"
          else
            allow_success_or_skipped "native-build" "$NATIVE_BUILD_RESULT"
          fi

          if [[ "$MARKDOWN_ONLY" == "true" ]]; then
            allow_success_or_skipped "dependency-audit" "$DEPENDENCY_AUDIT_RESULT"
            allow_success_or_skipped "native-test" "$NATIVE_TEST_RESULT"
            allow_success_or_skipped "native-e2e" "$NATIVE_E2E_RESULT"
            allow_success_or_skipped "native-web" "$NATIVE_WEB_RESULT"
            allow_success_or_skipped "native-node" "$NATIVE_NODE_RESULT"
            allow_success_or_skipped "native-images" "$NATIVE_IMAGES_RESULT"
            allow_success_or_skipped "native-desktop" "$NATIVE_DESKTOP_RESULT"
            allow_success_or_skipped "kind-e2e" "$KIND_E2E_RESULT"
            allow_success_or_skipped "docker-e2e" "$DOCKER_E2E_RESULT"
            allow_success_or_skipped "postgres-security" "$POSTGRES_SECURITY_RESULT"
          else
            require_success "dependency-audit" "$DEPENDENCY_AUDIT_RESULT"
            require_success "native-test" "$NATIVE_TEST_RESULT"
            require_success "native-e2e" "$NATIVE_E2E_RESULT"
            require_success "native-web" "$NATIVE_WEB_RESULT"
            require_success "native-node" "$NATIVE_NODE_RESULT"
            require_success "native-images" "$NATIVE_IMAGES_RESULT"
            require_success "native-desktop" "$NATIVE_DESKTOP_RESULT"
            require_success "kind-e2e" "$KIND_E2E_RESULT"
            require_success "docker-e2e" "$DOCKER_E2E_RESULT"
            require_success "postgres-security" "$POSTGRES_SECURITY_RESULT"
          fi
      - uses: actions/checkout@v6
        if: always()
      - name: Measure complete CI critical path against the 30-minute budget
        if: always()
        env:
          CI_PERFORMANCE_PREFIX: ${{ inputs.performance_prefix || '' }}
          GH_TOKEN: ${{ github.token }}
          COLD_CACHE: ${{ github.event.inputs.cold_cache }}
        run: |
          set -euo pipefail
          mkdir -p target/ci-performance
          gh api --paginate --slurp "repos/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID/attempts/$GITHUB_RUN_ATTEMPT/jobs?per_page=100" > target/ci-performance/jobs.json
          args=()
          if [[ "$COLD_CACHE" == true ]]; then args+=(--cold); fi
          status=0
          python3 scripts/ci-performance.py --jobs target/ci-performance/jobs.json \
            --run-id "$GITHUB_RUN_ID" --attempt "$GITHUB_RUN_ATTEMPT" \
            --job-prefix "$CI_PERFORMANCE_PREFIX" \
            --budget-minutes 30 --output target/ci-performance "${args[@]}" || status=$?
          if [[ -f target/ci-performance/performance.md ]]; then
            cat target/ci-performance/performance.md >> "$GITHUB_STEP_SUMMARY"
          fi
          exit "$status"
      - uses: actions/upload-artifact@v6
        if: always()
        with:
          name: ci-performance-${{ github.run_attempt }}
          path: target/ci-performance/performance.*
          if-no-files-found: warn
          retention-days: 14
