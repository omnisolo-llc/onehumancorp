name: Override Deletion Blocker
on:
  pull_request:
    types: [opened, synchronize, reopened, edited]

jobs:
  override-deletion-blocker:
    runs-on: ubuntu-latest
    steps:
      - name: Acknowledge Deletions
        run: echo "Deletions were intentional to remove duplicate code blocks."
