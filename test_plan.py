plan = """Awesome! The tests pass!
Now, I need to format my reply to the PR comment and use the `submit` tool.

The PR comment ID is: `4259968322`.
I will reply:
"I have investigated and fixed the failing test caused by the unsupported `IF NOT EXISTS` syntax in the SQLite migration for `033_swarm_tasks_kairos.sql`. All backend tests are now passing successfully."
"""
print(plan)
