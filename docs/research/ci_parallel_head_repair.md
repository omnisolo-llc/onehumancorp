# Required CI head repair — 2026-09-20

Baseline: main `15a913e27462c939e804c1863d5e4d659e74c93d`.

The owner requested minimum PR evaluation latency and supplied a 60-concurrent-runner quota. This iteration makes strict headless Rust lint and Node lint/typechecks independent required jobs, runs 32 isolated single-worker browser shards without retries, and reconciles every result against actual unsharded Playwright discovery. The graph stays below the supplied quota; the 30-minute full-CI budget is unchanged. Full Cargo test execution and Tauri lint/tests remain mandatory. The unchanged serial Rust test lane still needs measured optimization; separating lint alone does not establish the final timing target.

Hosted reports bind the exact source SHA, run ID, and attempt. Missing shards, duplicate/missing tests, skipped/interrupted tests, failed-first-attempt retries and incomplete reports fail qualification. Preflight uses structured discovery rather than depending on a console summary surviving large output. All browser specs under `./src` remain selected. A 20-minute Playwright execution deadline precedes the 22-minute wrapper limit and 25-minute hosted limit, preserving time to report failure and clean up this run's owned processes/containers.

Browser contract repairs in this iteration: the shared smoke helper now checks the documented 8-pixel shell surface contract (see `docs/superpowers/specs/2026-07-12-universal-ui-shell-design.md`), marketplace headings are scoped to the main content instead of colliding with the global shell title, mobile assistant checks use the maintained route and assert real layout rather than a conditional invalid selector, the exit-intent test uses the isolated stack's configured origin, and both tests named UI login actually submit the anonymous login form and verify returned role/tenant identity. These do not certify unrelated feature journeys.

Local evidence before first functional push: 120/120 native Node script tests passed, repository ESLint passed with zero warnings, cache/startup/security workflow contracts passed, and actual Playwright discovery accounted for all 1,688 tests exactly once across 32 shards. Discovery is not execution. No local Docker or Rust toolchain was available. Browser and Rust execution, guardrail endpoint integration, remaining legacy UI contracts, final full-CI timing and cross-platform release execution remain outstanding until recorded hosted results prove them.

The host checkout was not touched because the WebCodex executor was unavailable. Work used an isolated exact-main source tree with verified Git object identity. Temporary source/dependency transfer workflows are excluded from this resulting source tree. No changes were merged to main, deployed, published as releases, or made to branch protection, secrets, or live data.
