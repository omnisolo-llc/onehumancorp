# CUJ: Dynamic Cloud Escalation for Hybrid MCP RAG

## User Journey
As a user executing a computationally intensive MCP RAG query locally, when the query exceeds a specific telemetry threshold, the system should automatically escalate the task to the cloud swarm via a PostgreSQL integration, ensuring scale without local CPU bottlenecks, while maintaining privacy and providing visual indication of the execution state.
