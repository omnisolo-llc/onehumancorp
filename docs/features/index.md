# Features

Feature documentation is organized by capability area. Each mature feature area should include:

- A design document
- A CUJ
- A test plan
- A user guide when the capability is operator-facing

The feature tree is the right place for product-level behavior. Cross-cutting platform design belongs in `docs/architecture/`.
