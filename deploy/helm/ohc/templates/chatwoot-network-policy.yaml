apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ .Release.Name }}-chatwoot-isolation
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Release.Name }}-chatwoot
spec:
  podSelector:
    matchLabels:
      app: {{ .Release.Name }}-chatwoot
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: {{ .Release.Namespace }}
      podSelector:
        matchLabels:
          app: {{ .Release.Name }}-ohc-core
    ports:
    - protocol: TCP
      port: 80
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: {{ .Release.Namespace }}
      podSelector:
        matchLabels:
          app: {{ .Release.Name }}-ohc-core
