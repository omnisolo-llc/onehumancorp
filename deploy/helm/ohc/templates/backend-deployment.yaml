{{- $backendEnv := default dict .Values.backend.env -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-backend
spec:
  replicas: {{ .Values.backend.replicas }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: {{ .Release.Name }}-backend
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-backend
    spec:
      containers:
        - name: backend
          image: {{ .Values.backend.image }}
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
            capabilities:
              drop:
                - "ALL"
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          resources:
            {{- toYaml .Values.backend.resources | nindent 12 }}
          ports:
            - containerPort: {{ .Values.backend.service.port }}
          env:
            - name: OHC_LOG_FORMAT
              value: "json"
            - name: OHC_LOG_LEVEL
              value: "info"
            - name: OHC_PORT
              value: "{{ .Values.backend.service.port }}"
            {{- if .Values.redis.enabled }}
            - name: OHC_REDIS_ADDR
              value: "{{ .Release.Name }}-redis-master:6379"
            {{- end }}
            {{- if .Values.cnpg.enabled }}
            - name: OHC_DATABASE_URL
              value: "postgres://{{ .Values.cnpg.owner }}:$(POSTGRES_PASSWORD)@{{ .Values.cnpg.clusterName }}-rw:5432/{{ .Values.cnpg.database }}?sslmode=require"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.cnpg.clusterName }}-app
                  key: password
            {{- end }}
            {{- if .Values.chatwoot.enabled }}
            - name: OHC_CHATWOOT_ENABLED
              value: "true"
            - name: OHC_CHATWOOT_URL
              value: "http://{{ .Release.Name }}-chatwoot:{{ .Values.chatwoot.service.port }}"
            - name: OHC_CHATWOOT_ADMIN_EMAIL
              value: "{{ .Values.chatwoot.adminEmail }}"
            - name: OHC_CHATWOOT_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-chatwoot-secret
                  key: adminPassword
            {{- end }}
            - name: OHC_MULTITENANT
              value: "{{ .Values.multiTenant.enabled }}"
            - name: OHC_HEADLESS
              value: "{{ .Values.backend.headless }}"
            {{- range $name, $value := $backendEnv }}
            - name: {{ $name }}
              value: {{ $value | quote }}
            {{- end }}
            {{- if .Values.ohcCore.enabled }}
            - name: OHC_CORE_URL
              value: "http://{{ .Release.Name }}-ohc-core:{{ .Values.ohcCore.service.port }}"
            {{- end }}
            {{- if not (hasKey $backendEnv "OHC_JWT_SECRET") }}
            - name: OHC_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-backend-secret
                  key: jwtSecret
                  optional: true
            {{- end }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: {{ .Values.backend.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: {{ .Values.backend.service.port }}
            initialDelaySeconds: 3
            periodSeconds: 5
      volumes:
        - name: tmp
          emptyDir: {}
