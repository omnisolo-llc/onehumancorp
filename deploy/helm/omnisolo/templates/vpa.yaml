{{- if .Values.backend.vpa.enabled }}
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: {{ include "omnisolo.fullname" . }}-backend-vpa
  labels:
    {{- include "omnisolo.labels" . | nindent 4 }}
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "omnisolo.fullname" . }}-backend
  updatePolicy:
    updateMode: "Auto"
{{- end }}
---
{{- if and .Values.omnisoloCore.enabled .Values.omnisoloCore.vpa.enabled }}
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: {{ include "omnisolo.fullname" . }}-core-vpa
  labels:
    {{- include "omnisolo.labels" . | nindent 4 }}
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "omnisolo.fullname" . }}-core
  updatePolicy:
    updateMode: "Auto"
{{- end }}
---
{{- if and .Values.powersync.enabled .Values.powersync.vpa.enabled }}
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: {{ include "omnisolo.fullname" . }}-powersync-vpa
  labels:
    {{- include "omnisolo.labels" . | nindent 4 }}
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "omnisolo.fullname" . }}-powersync
  updatePolicy:
    updateMode: "Auto"
{{- end }}
