rules:
  - name: Global
    sync_queries:
      - name: agent_missions
        query: SELECT * FROM agent_missions WHERE organization_id = current_setting('request.jwt.claim.organization_id', true)
      - name: tenant_registry
        query: SELECT * FROM tenant_registry WHERE id = current_setting('request.jwt.claim.organization_id', true)
