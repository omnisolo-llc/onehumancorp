# sync_rules.yaml
bucket_definitions:
  user_bucket:
    # Filter by organization_id provided in the JWT
    parameters:
      - select request.jwt()->>'organization_id' as user_id
    data:
      - select * from agent_missions where organization_id = bucket.user_id
      - select * from tenant_registry where organization_id = bucket.user_id
