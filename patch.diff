--- srcs/server/tools/mcpwebhooktunnel/mcp_test.go
+++ srcs/server/tools/mcpwebhooktunnel/mcp_test.go
@@ -87,7 +87,15 @@
	defer client.Close()

	// Wait for the connection to be established and registered
-	time.Sleep(200 * time.Millisecond)
+	for i := 0; i < 50; i++ {
+		res, err := client.CallTool(ctx, "get_tunnel_status", nil)
+		if err == nil {
+			if statusMap, ok := res.(map[string]interface{}); ok && statusMap["status"] == "connected" {
+				break
+			}
+		}
+		time.Sleep(10 * time.Millisecond)
+	}
+	time.Sleep(100 * time.Millisecond) // Give the server a little more time to register the stream in r.streams

	tools := client.ListTools()
	if len(tools) != 1 {
