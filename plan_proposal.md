1. **Create Database Migration**: Create `srcs/server/db/migrations/032_add_hybrid_sync_metadata.sql` that adds `sync_status` and `last_sync_at` to the `autodream_memories` table (as well as the primary RAG context table `swarm_memory_embeddings` if needed, based on the prompt "rag_memories table (assuming such a table exists, or the primary context table)", `swarm_memory_embeddings` is from `005_sip.sql` and `autodream_memories` is from `024_autodream_memories.sql` and `029_autodream_memories.sql`. Wait, `swarm_memory_embeddings` is the original RAG table. I will add the columns to both just in case, but prioritize `swarm_memory_embeddings` based on `005_sip.sql` context and `autodream_memories`). I will use `ALTER TABLE ADD COLUMN` queries.
2. **Update `BUILD.bazel`**: Add the new migration `032_add_hybrid_sync_metadata.sql` to `srcs/server/db/BUILD.bazel`.
3. **Implement Go Interface & Structs**: Create `srcs/server/hub/rag_sync.go` with `SyncStatus`, `RAGSyncRecord`, and `RAGSyncService` interface. Include `vector_embedding` conversion handling (`[]byte` vs `[]float32` vs string based on schema compatibility). Since the prompt specifies `Vector []float32`, I will stick with the interface requested. Add metrics using OpenTelemetry (`rag_records_synced_total`, `rag_sync_errors_total`).
4. **Create BUILD.bazel for Hub**: Create `srcs/server/hub/BUILD.bazel` since the directory is new or lacks a BUILD file.
5. **Implement Unit Tests**: Create `srcs/server/hub/rag_sync_test.go` to mock the service and test the data flow, verifying basic logic as requested.
6. **Run Tests & Verify**: Run `bazelisk test //srcs/server/hub/... //srcs/server/db/...` to verify changes.
7. **Complete Pre-Commit Steps**: Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
8. **Finalize Mission & Submit**: Mark mission as DONE in `.agent-task/missions/2026-04-07T08-02-24Z_hybrid_mcp_rag_sync.md` and submit PR using the standard title format `🚀 Nova: [feature]`.
