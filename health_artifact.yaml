issue_category: security
triage:
  - Addressed postgres database mock connection configuration failing natively.
  - Restored file::memory: caching parameters in SQLite db setup configurations for API tests to ensure no isolation gaps or concurrent deadlocks when running unit tests.
  - Implemented Chaos tests covering Redis Mailbox corruption, .agent-lock/ race conditions, dropping pub/sub blocks, and degrading backend >2s latency fail-safes.
debt_report: "No manual tags remain in Bazel suite. Fully executing hermetic tests."
