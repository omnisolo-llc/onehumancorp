issue_category: "bug"
debt_report: |
  <style>
    .debt-report {
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(15px);
      -webkit-backdrop-filter: blur(15px);
      border-radius: 12px;
      padding: 24px;
      font-family: 'Inter', sans-serif;
      color: #333;
    }
  </style>
  <div class="debt-report">
    <h3>Debt Report</h3>
    <p>Aligned mismatching schema variables (like <code>assigned_agent_id</code>), regenerated protobuf definitions, and handled chaotic database failures more resiliently in testing. No domain tasks specified directly in instructions aside from testing, so zero WIP committed.</p>
  </div>
