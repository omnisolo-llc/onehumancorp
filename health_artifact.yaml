triage_status: completed
issue_category: refactor
debt_report: |
  Optimized Kubernetes scaling limits (HPA/VPA) to reduce baseline replica count to save costs in multi-tenant environments.
  Hardened overall Kubernetes ResourceQuotas.
  Reduced the footprint limits of the local standalone wrapper via Docker Compose limits.
  Tweaked Prometheus scrape interval to 60s for the lightweight agent.
  Applied Glassmorphism design tag natively to all Grafana dashboards.
