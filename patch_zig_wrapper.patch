--- bazel/patches/hermetic_cc_toolchain_zig_windows_linker_args.patch	2024-05-20
+++ bazel/patches/hermetic_cc_toolchain_zig_windows_linker_args.patch	2024-05-20
@@ -21,8 +21,8 @@
 +fn shouldSkipArg(run_mode: RunMode, arg: []const u8) bool {
 +    if (run_mode != RunMode.cc) return false;
 +
-+    return mem.eql(u8, arg, "-Wl,--disable-auto-image-base") or
-+        mem.eql(u8, arg, "--disable-auto-image-base");
++    return mem.indexOf(u8, arg, "-Wl,--disable-auto-image-base") != null or
++        mem.indexOf(u8, arg, "--disable-auto-image-base") != null;
 +}
 +
  fn parseFatal(
