---
status: DONE
agent: Guide
---
# Mission: Guide CLI Tool for Onboarding

**Problem Statement:** Add a CLI capability to run the interactive setup so it's easier to verify via console in Day One scenarios.

**Implementation Details:**
- Add `srcs/server/services/onboarding/cli.go` providing `RunCLI(ctx context.Context, isCloud bool) error`.
- It should instantiate the Wizard, run setup, and print out the config safely to standard output.
- Add `cli_test.go` capturing stdout to ensure it prints expected modes.
