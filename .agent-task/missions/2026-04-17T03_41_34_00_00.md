---
status: BLOCKED
agent: Implementer
---

# Title: Implement CRDT MCP Synchronization Logic

## Problem Statement
Bridging Standalone (local SQLite) with Cloud-Native (multi-tenant PostgreSQL) environments requires sophisticated state resolution. Agents modifying shared tasks offline must seamlessly synchronize with the cloud once network connectivity is restored.

## Research Report
According to `docs/architecture/hybrid_crdt_sync_blueprint.md`, a Conflict-free Replicated Data Type (CRDT) abstraction layer via the Model Context Protocol (MCP) should be introduced. This allows agents to operate autonomously on local data copies, using `crdt_pull` and `crdt_push` tools to eventually synchronize state with the K8s-orchestrated backend.

## Design Doc
Architecture:
*   **Standalone Mode:** SQLite logs state changes (deltas). A lightweight Go-based MCP tool periodically reads these and pushes to Cloud.
*   **Cloud Mode:** Ingests deltas into Postgres, resolving conflicts based on `updated_at`.
*   **API Contract:** `/api/v1/sync/mcp-deltas` (POST).

The cloud ingestion endpoint will accept an array of CRDT deltas:
```json
{
  "deltas": [
    {
      "id": "delta_123",
      "entity_id": "task_abc",
      "data": "{\"status\": \"completed\"}",
      "updated_at": "2026-04-17T12:00:00Z"
    }
  ]
}
```

## Implementation Prompt
Hello Implementer! Your objective is to build the CRDT MCP synchronization logic based on `docs/architecture/hybrid_crdt_sync_blueprint.md`.
1. Implement the local SQLite tracking (deltas).
2. Implement a Go-based MCP tool for `crdt_pull` and `crdt_push`.
3. Implement the Cloud Postgres ingestion endpoint `POST /api/v1/sync/mcp-deltas`.
4. Add unit and integration tests.
5. Provide a summary of the implementation via a new GitHub PR.

## Priority
P0

## Estimated Scope
Large

blockers:
  - Outside Wizard/Onboarding domain
