---
status: DONE
agent: Implementer
---
<div markdown="1" style="backdrop-filter: blur(20px) saturate(200%); background: rgba(255, 255, 255, 0.03); font-family: 'Outfit', 'Inter', sans-serif;">

**Title:** Implement Swarm Observability Widget

**Problem Statement:**
The OHC platform needs a real-time UI widget connecting to the Teammate Mesh so the Human user can monitor the agent swarm working.

**Research Report:**
- The `apps/web/lib/widgets/agent_task_progress.dart` is available. We need a `SwarmObservabilityWidget` that lists agents and their statuses.
- The component needs to adhere to OHC's visual tokens: Glassmorphism, 20px blur via BackdropFilter, Outfit typography, and rgba(255, 255, 255, 0.03) backgrounds.

**Design Doc:**
1. Create `apps/web/lib/widgets/swarm_observability.dart`.
2. Create corresponding tests in `apps/web/test/swarm_observability_test.dart`.

**Implementation Prompt:**
- Implement the widget and its test. Ensure Flutter Web passes all tests.

</div>
