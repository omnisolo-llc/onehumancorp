---
status: PENDING
---

# Title: Implement Final KAIROS AI OS Orchestration Architecture

**Problem Statement**:
The OHC Hybrid Agentic OS lacks a fully unified implementation of its core master orchestration systems (Phase 1-4). Previous implementations relied on fragmented documents, causing discrepancies between Cloud-Native and Standalone behaviors (particularly regarding `FOR UPDATE SKIP LOCKED` vs SQLite Go Mutexes, and Redis `rueidis` vs Memory Bus). A centralized architecture standard has been established, and the implementations must now be updated to perfectly align with it.

**Research Report**:
According to recent KAIROS master orchestration findings, Swarm Intelligence requires four distinct phases to operate autonomously without human bottleneck:
1. Shared Task List (`kairos_shared_tasks`) for DAG feature decomposition.
2. Realtime Teammate Mesh APIs for agent-to-agent Pub/Sub orchestration.
3. AutoDream pipelines for pgvector memory consolidation.
4. Sub-Agent queue management.
Current codebase inconsistencies violate the OHC-SIP payload constraints for Teammate Mesh and require full unification.

**Design Doc**:
Refer exclusively to `docs/architecture/KAIROS_AI_OS_MASTER_ORCHESTRATION_FINAL.md`. This master architectural document contains the finalized PostgreSQL schemas, OHC-SIP payload contracts (`agent_id`, `channel`, `event_type`, `data`), and the expected execution flows for all four phases.

**Implementation Prompt**:
Hello Implementer agent. Your task is to apply the final architectural blueprints to the existing implementation:
1. Read the comprehensive architecture in `docs/architecture/KAIROS_AI_OS_MASTER_ORCHESTRATION_FINAL.md`.
2. Update the shared task database schemas across both Cloud (PostgreSQL `FOR UPDATE SKIP LOCKED`) and Standalone (SQLite WAL + Mutex).
3. Ensure the Teammate Mesh API strictly uses the exact OHC-SIP payload contract (`agent_id`, `channel`, `event_type`, `data`).
4. Validate that the AutoDream `pgvector` worker correctly embeds completed task histories.
5. Create comprehensive automated tests ensuring >90% coverage for the Queue Manager.

**Priority**: P0
**Estimated Scope**: Large
