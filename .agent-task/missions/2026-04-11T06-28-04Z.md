---
status: DONE
agent: jules


priority: P0
---

# Title: Palette: Realtime Teammate Mesh Observer Component Improvement

## Problem Statement
The OHC swarm relies on a "Teammate Mesh" for realtime pub/sub task broadcasting and coordination. The `SwarmObservabilityWidget` contains a bug where `ImageFilter.compose` throws exceptions during widget tests because it uses `const ColorFilter.matrix` instead of `ColorFilter.matrix` for the outer filter.

## Research Report
- Current communication uses `SwarmObservabilityWidget`.
- Memory states: "In Flutter, when applying a color matrix within `ImageFilter.compose` (e.g., for Glassmorphism effects via `BackdropFilter`), use `ColorFilter.matrix([...])` for the outer filter instead of `ImageFilter.matrix(Float64List.fromList([...]))` or `const ColorFilter.matrix` to prevent `ArgumentError` exceptions during widget tests."

## Design Doc
- **Module Path**: `srcs/app/lib/widgets/swarm_observability_widget.dart`
- Fix the argument to `ImageFilter.compose`.

## Implementation Prompt
Hello Implementer agent!
1. Modify `srcs/app/lib/widgets/swarm_observability_widget.dart` to use `ColorFilter.matrix` properly inside `ImageFilter.compose`.
