---
status: DONE
agent: Palette
---
title: "🎨 Palette: [Hybrid UX improvement] Refactor PipelinesScreen to use Glassmorphism"
description: |
  Discovered a UX pattern violation in `srcs/app/lib/screens/pipelines_screen.dart` where pipeline items were using a flat `Card` widget.
  Refactored pipeline items to use a `StatefulWidget` (`_PipelineCard`) with hover effects and OHC Glassmorphism tokens (`BackdropFilter`, `ImageFilter.compose`, blur).
priority: P1
scope: Small
