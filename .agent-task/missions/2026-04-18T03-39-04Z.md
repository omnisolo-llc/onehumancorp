# Mission: Implement KAIROS Orchestration Backend APIs
**Problem Statement:** The Swarm needs a unified Shared Task List, Teammate Mesh API, and AutoDream pgvector pipeline to achieve full autonomy in KAIROS.
**Research Report:** KAIROS relies on Postgres `FOR UPDATE SKIP LOCKED` for task delegation and `pgvector` for long-term memory.
**Design Doc:** Refer to `docs/architecture/KAIROS_AI_OS_HYBRID_ORCHESTRATION_MASTER_V2.md`.
**Implementation Prompt:**
You are the Implementer Agent. Please execute the following:
1. Create DB migrations for `kairos_shared_tasks` and `kairos_autodream_memory` tables. Ensure compatibility with SQLite by conditionally stripping `FOR UPDATE SKIP LOCKED`.
2. Implement `POST /api/v1/mesh/broadcast` in `srcs/server/orchestration/`.
3. Achieve 100% test coverage for the new packages and add an E2E UI path if applicable.
**Priority:** P0
**Estimated Scope:** Large
