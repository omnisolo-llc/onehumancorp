---
status: "PENDING"
Title: "Implement KAIROS Phase 1, 2, and 3: Shared Task List, Teammate Mesh, and AutoDream"
Priority: "P0"
Estimated Scope: "Large"
---

# Problem Statement
The KAIROS Orchestration architecture has been finalized. We must now implement the database logic for the Shared Task List, real-time Teammate Mesh integration via CentrifugeNode and Redis, and the AutoDream vector data pipelines for long-term memory.

# Research Report
- Architecture is detailed in:
  - `docs/features/kairos/shared_task_list.md`
  - `docs/features/kairos/teammate_mesh.md`
  - `docs/features/kairos/autodream_pipeline.md`
- Implementation must support hybrid multi-tenant PostgreSQL (with `pgvector`) and standalone SQLite execution modes.

# Design Doc
Refer to the master orchestration documentation linked above for SQL schemas and sequence diagrams.

# Implementation Prompt
You are an Implementer agent. Your task is to:
1. Implement the Shared Task List database schema and `ClaimTask` using `FOR UPDATE SKIP LOCKED` in PostgreSQL or local locking for SQLite (`srcs/server/orchestration/tasks_db.go`).
2. Implement the Teammate Mesh APIs leveraging `CentrifugeNode` and `rueidis` for pub/sub (`srcs/server/orchestration/hub.go`). Add OpenTelemetry instrumentations.
3. Implement the `AutoDreamWorker` memory consolidation pipeline and `autodream_memories` table (`srcs/server/orchestration/autodream_pipeline.go`). Use `LIMIT 500` for chunking.
4. Add all migration files to `srcs/server/db/migrations/`.
5. Ensure robust error handling, test parity between Cloud/Standalone modes, and execute `bazelisk test //srcs/server/orchestration/...`.
