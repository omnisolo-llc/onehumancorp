---
status: DONE
agent: jules
---
Title: "Implement Realtime Teammate Mesh APIs"
Priority: "P0"
Estimated Scope: "Large"
---

# Problem Statement
The OHC swarm needs realtime communication.

# Research Report
Phase 2 of KAIROS orchestration demands WebSockets/gRPC with Redis Pub/Sub backplane for Cloud-Native and memory channels for Standalone.

# Design Doc
Realtime APIs over mesh channels. Provide Redis and Memory transports.

# Implementation Prompt
You are an Implementer agent. Your task is to:
1. Implement `MeshTransport` interface in `srcs/server/orchestration/hub.go`.
2. Provide `RedisMeshTransport` and `MemoryMeshTransport` with methods `Publish(topic string, data []byte)` and `Subscribe(topic string)`.
3. Ensure the orchestration hub supports mesh events in `srcs/proto/hub.proto`.
4. Test and verify expected sub-millisecond delivery via `bazelisk test //...`.
