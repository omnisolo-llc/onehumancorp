---
status: DONE
---
<div markdown="1" style="backdrop-filter: blur(20px) saturate(200%); background: rgba(255, 255, 255, 0.03); font-family: 'Outfit', 'Inter', sans-serif;">

# Title: Implement Referral Service for Viral Growth Loop

## Problem Statement
We need a robust referral system in the `services/growth` directory to track viral invites sent and accepted.

## Research Report
A simple API using the existing `lib/analytics` tracker will allow tracking.

## Design Doc
1.  **Referral Service**: Exposes `ProcessInvite` and `AcceptInvite`.
2.  **Tracking**: Calls OpenTelemetry tracker for `invite_sent` and `invite_accepted`.

## Implementation Prompt
Implemented `ReferralService` and associated tests.

## Priority
P0

## Estimated Scope
Small

</div>
