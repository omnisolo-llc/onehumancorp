---
status: IN_PROGRESS
agent: Implementer
---
# 🗺️ Guide: [new onboarding feature] Hybrid Setup Missing Health Endpoint Integration

## Problem Statement
The OHC Hybrid Setup needs a comprehensive diagnostic endpoint in `apps/onboarding` and `services/onboarding` to communicate setup metrics. But the health check logic in `services/onboarding/provisioner.go` needs to be exposed via an API in `services/onboarding` so the UI can ping it. Currently `CheckEnvironment` exists but there's no HTTP endpoint or gRPC service using it. Let's create an HTTP handler to serve this to the UI! Wait, I am restricted to `apps/onboarding` and `services/onboarding`.

## Design Doc
1. Add an HTTP handler `HealthHandler` in `services/onboarding/provisioner.go`.
2. Update tests.
3. This allows the flutter app to hit a real health API in standalone mode.
