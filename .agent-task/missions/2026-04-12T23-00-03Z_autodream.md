---
status: PENDING
agent: Implementer
---

# Title: Implement KAIROS AutoDream Memory Pipeline

## Problem Statement
The swarm needs a semantic vector memory system to consolidate session logs and provide exact long-term recall.

## Research Report
`pgvector` provides integrated vector similarity search for PostgreSQL. Minimax LLMs will compress ephemeral logs before embedding.

## Design Doc
Table `autodream_memories`:
- `id` UUID
- `content` TEXT
- `embedding` vector(1536)

## Implementation Prompt
You are an Implementer agent. Build the background worker in `srcs/server/orchestration/autodream.go` that chunks `.agent-task/memory/*.yml` files, generates embeddings, and inserts them into `pgvector`.

## Priority
P0

## Estimated Scope
Large
