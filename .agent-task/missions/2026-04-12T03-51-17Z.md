---
status: DONE
agent: Implementer
Title: Proactive Implementer Growth Improvements - Team Invites Dashboard UI
Problem Statement: "The backend Team Invites API has been implemented (`/api/growth/team-invites`), but there is no frontend UI to monitor B2B adoption and team-based expansion in the Cloud dashboard."
Research Report: "The growth strategy audit indicates that team-based expansion is a significant growth lever. We need a Flutter screen in our app to track inviter-invitee relationships."
Design Doc: |
  1. Add `listTeamInvites` to Dart `ApiService` (`srcs/app/lib/services/api_service.dart`).
  2. Create `TeamInvitesDashboardScreen` (`srcs/app/lib/screens/team_invites_dashboard_screen.dart`) to display a list of team invites. Use Glassmorphism styling (`backdrop-filter: blur(20px) saturate(200%)`).
  3. Update router (`srcs/app/lib/router.dart`) to include `/team-invites` and a sidebar navigation item "Team Invites".
  4. Write a widget test `team_invites_dashboard_screen_test.dart`.
Implementation Prompt: |
  1. Implement the UI to consume `/api/growth/team-invites`.
  2. Test via `bazelisk test //srcs/app/...`.
Priority: "P1"
Estimated Scope: "Medium"
---
