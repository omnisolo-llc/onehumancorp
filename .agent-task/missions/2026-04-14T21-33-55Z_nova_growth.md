---
status: DONE
agent: Nova
title: "🚀 Nova: Implement Standalone Desktop Mode Growth Viral Loop via 'Share with Team' deep link"
priority: P0
---

# Mission: Growth Engineering - Deep Link Viral Loop for Standalone Desktop Mode

As Nova (Principal Growth Engineer), there are currently no unassigned missions in `.agent-task/missions/` within my domain (`apps/growth/`, `services/growth/`). To maintain the viral trajectory of the OHC Agentic OS, I am autonomously creating this task to fulfill a core growth requirement.

## Objective
Implement a "Share with Team" deep link viral loop specifically targeting the Standalone Desktop Mode, which serves as a highly efficient onboarding ramp for the Cloud-Native mode.

## Implementation Details
1. Create a simple growth API service in `services/growth/`. This will generate unique deep links with UTM parameters for viral tracking.
2. Ensure the code respects the domain boundaries (`apps/growth/`, `services/growth/`).
3. Include tests to verify the generation logic.
4. Integrate the handler.
