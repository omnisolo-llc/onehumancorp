---
status: PENDING
priority: P0
scope: Large
---
# Phase 1: Shared Task List, DAG Dependencies, and State Machines

**Problem Statement**: The OHC Swarm needs an autonomous backend architecture to track and orchestrate multi-agent DAG flows without stepping on each other.

**Research Report**: We need a robust distributed state machine backed by Postgres (`shared_tasks`, `task_dependencies`) with `FOR UPDATE SKIP LOCKED`. These were confirmed via `docs/KAIROS_AI_OS_ARCHITECTURE.md`.

**Design Doc**: See `docs/KAIROS_AI_OS_IMPLEMENTATION_DESIGN.md`.

**Implementation Prompt**: Update or verify DB schemas for `shared_tasks` and `task_dependencies` in `srcs/server/db/migrations/` to ensure they match `docs/KAIROS_AI_OS_ARCHITECTURE.md`. Implement DAG dependency management logic for task creation and locking. Update testing files. Ensure pg/sqlite compatibility in `srcs/server/db/`.
