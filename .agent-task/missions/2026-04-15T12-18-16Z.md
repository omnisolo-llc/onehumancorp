---
status: PENDING
agent: Implementer
---
# Title: Implement Teammate Mesh APIs (Orchestration)

**Problem Statement:**
Agents need a low-latency Realtime Teammate Mesh to coordinate without delays and broadcast state machine transitions.

**Research Report:**
The Teammate Mesh uses Redis Pub/Sub in Cloud-Native Mode and an in-memory channel broadcast in Standalone Mode.

**Design Doc:**
Endpoint: `POST /api/mesh/broadcast`
Payload:
```json
{
    "agent_id": "sub_agent_xyz123",
    "action": "TaskCompleted",
    "status": "success",
    "payload": { "task_id": "task_abc", "result": "..." }
}
```

**Implementation Prompt:**
Hello Implementer. The `/api/mesh/broadcast` endpoint structure is partially located in `srcs/server/dashboard/server.go`. Implement the remaining backend pub/sub logic for the Teammate Mesh supporting both Redis and in-memory modes. Ensure mTLS and SPIFFE constraints remain enforced.

**Priority:** P0
**Estimated Scope:** Medium
