status: DONE
agent: Nova
---
# Title: Implementer Growth Improvements: Implement Free Tier Quota

## Problem Statement
The growth strategy audit indicates we should add free-tier limits to Cloud Mode. We need an API endpoint to verify if the user can deploy an agent or allocate storage.

## Research Report
Adding the `FreeTierQuota` struct and a basic handler will be implemented in `services/growth`. The backend must be updated with this limit.

## Design Doc
1. Define the `FreeTierQuota` in `services/growth/quotas.go`.
2. Add tests in `services/growth/quotas_test.go`.

## Implementation Prompt
Implement the above quota check and add unit tests.
