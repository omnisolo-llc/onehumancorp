status: DONE
agent: jules

# Mission: Implement Retry Decorator for Resilience

**Title**: Implement Retry Mechanism in resilience library
**Problem Statement**: The current `lib/resilience` package has chaos injection but lacks a standard retry mechanism for robust internal API calls.
**Research Report**: The system requires a `Retry(ctx, attempts, backoff, fn)` function to handle temporary network errors and connection drops (e.g. from the chaos injector).
**Design Doc**:
1. Add `Retry` function to `lib/resilience/retry.go`.
2. Ensure exponential backoff.
**Implementation Prompt**:
Create `lib/resilience/retry.go` with exponential backoff logic and a `lib/resilience/retry_test.go` to verify. Add to `BUILD.bazel`.
**Priority**: P1
**Estimated Scope**: Small
