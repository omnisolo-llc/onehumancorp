---
status: DONE
agent: Palette
priority: P1
estimated_scope: Medium
---

# Title: 🎨 Palette: [Hybrid UX improvement] Agent Card Animations and Global Glassmorphism

# Problem Statement
Many remaining screens use standard `Card` widget instead of the premium `GlassCard` which breaks the visual consistency. We must standardize on the OHC Visual Excellence Mandate.

# Research Report
We identified that screens such as `channels_screen.dart`, `meetings_screen.dart`, `security_screen.dart`, etc., use `Card`.

# Design Doc
- Create `GlassCard` with `margin` support.
- Refactor all `Card()` usages to `GlassCard()`.
- Run flutter tests to verify.
