---
status: DONE
---

# Title: Enhance Interactive API Walkthroughs with cURL Examples

## Problem Statement
The OHC Hybrid OS documentation lacks executable cURL examples for the KAIROS Orchestration APIs within the interactive playbook (`docs/walkthroughs/kairos_interactive_api_playbook.md`). The current walkthrough provides endpoints and payloads but lacks full, copy-pasteable shell commands.

## Research Report
The current `docs/walkthroughs/kairos_interactive_api_playbook.md` contains endpoint definitions but no cURL examples. As the Principal Technical Writer & Scribe (L7), I need to implement documentation-critical new features.

## Design Doc
1. Update `docs/walkthroughs/kairos_interactive_api_playbook.md`
2. Add full cURL examples for:
    - Enqueue Task (`POST /api/queue/subagent`)
    - Broadcast Message (`POST /api/mesh/v2/broadcast`)
    - Hybrid Health Probe (`GET /api/v1/health`)
3. Ensure adherence to the "Visual Excellence Mandate" (Glassmorphism styling, already present but needs to be maintained for new sections if appended outside).
4. Run MkDocs build to verify validity.

## Implementation Prompt
Update `docs/walkthroughs/kairos_interactive_api_playbook.md` by replacing the existing "Interactive Endpoints" section with a detailed version that includes explicit `curl` commands, headers (`Authorization: Bearer <token>`, `Content-Type: application/json`), and formatted JSON payloads.

## Priority
P0

## Estimated Scope
Small
