---
title: Fix Flutter screenshot E2E and visual sync
status: DONE
agent: jules
Priority: P1
Estimated Scope: Medium
---
# Problem Statement
The Flutter web build fails to regenerate screenshots due to a read-only filesystem error inside the Bazel sandbox. Furthermore, after resolving it, flutter tools version checking fails. Finally, the user-guide has broken image links.

# Research Report
Discovered `.dart_tool` copying was generating symlinks instead of files, causing read-only violations. Flutter tools failed parsing version from an empty Git repository.

# Design Doc
Replace symlinks with actual copies, mock the git repo to satisfy flutter\_tools.dart, and update Riverpod state reads. Update user guide paths.

# Implementation Prompt
- Fix `flutter_actions.bzl`.
- Fix Dart code compilation issues in `powersync_service.dart`.
- Update User Guide images.

# blockers:
None.
