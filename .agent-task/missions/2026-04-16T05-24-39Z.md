status: BLOCKED
agent: jules
blockers: Out of scope. Mission requests changes to srcs/server/ but my designated technical domain is exclusively apps/api/, services/, lib/.

# Mission: Implement KAIROS Hybrid Orchestration Phases

**Title**: Implement KAIROS Hybrid Orchestration Core Infrastructure
**Problem Statement**: The current orchestrator lacks the explicit distributed PostgreSQL `shared_tasks_v2` schema for lock-safe parallel execution, and the Redis-backed Teammate Mesh API endpoints required for Phase 1 and Phase 2 of the KAIROS Hybrid OS.
**Research Report**: According to `docs/architecture/KAIROS_AI_OS_PREMIUM_DESIGN.md`, the Swarm requires a robust lock-safe task list (`FOR UPDATE SKIP LOCKED`) and a Redis Pub/Sub Teammate Mesh. Additionally, the Phase 3 autoDream pipeline requires a `pgvector` schema integration.
**Design Doc**:
1. **Phase 1**: Execute SQL migrations to create `shared_tasks_v2` table. Implement `tasks_db.go` with `ClaimPendingTask` using `FOR UPDATE SKIP LOCKED`.
2. **Phase 2**: Implement `BroadcastEvent` and `SubscribeChannel` in `srcs/server/orchestration/mesh.go` utilizing `rueidis` for Redis Pub/Sub.
3. **Phase 3**: Create the `ohc_memory.autodream_vectors` table migration and hook up the `pgvector` embedding ingestion loop in `srcs/server/orchestration/autodream_worker.go`.
**Implementation Prompt**:
Hello Implementer!
1. Create a migration file in `srcs/server/db/migrations/` for `shared_tasks_v2` and `ohc_memory.autodream_vectors`. Add it to `embedsrcs` in `BUILD.bazel`.
2. Update `srcs/server/orchestration/tasks_db.go` with a method `ClaimPendingTask(ctx context.Context) (*Task, error)` using the explicit `FOR UPDATE SKIP LOCKED` query defined in the design doc.
3. Implement the Redis Pub/Sub functions `BroadcastEvent` and `SubscribeChannel` in `srcs/server/orchestration/mesh.go`.
4. Ensure all changes pass linting and run `bazelisk test //srcs/server/orchestration/...`.
**Priority**: P0
**Estimated Scope**: Large
