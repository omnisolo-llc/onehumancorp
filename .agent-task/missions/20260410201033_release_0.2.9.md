---
title: "Release 0.2.9: Hybrid SemVer Bump and Promotion"
problem_statement: |
  We needed to promote the system to the 0.2.9 version following the addition of the new State Sync MCP proxy.
  This requires bumping SemVer across the frontend (Flutter), backend (Bazel/Go), Node (package.json), and Helm.
  We also need to document these changes in the release notes according to the OHC-SIP guidelines.
research_report: |
  Identified `srcs/app/pubspec.yaml`, `package.json`, `MODULE.bazel`, and `deploy/helm/ohc/Chart.yaml` as the versioning targets.
  Discovered `CHANGELOG.md` and `RELEASE_NOTES.md` required updates for the release, distinguishing between Cloud and Standalone.
design_doc: |
  - Updated all version files to `0.2.9` / `0.2.9+1`.
  - Added new release sections in markdown files.
  - Recorded telemetry.
implementation_prompt: |
  N/A
priority: "P0"
estimated_scope: "Small"
status: DONE
---