# Title: Implement KAIROS Core Hybrid Orchestration Architecture
**Problem Statement:**
The OHC Swarm lacks a unified implementation of the foundational orchestration architecture. Specifically, we need to enforce the structural and aesthetic vision for the Shared Task List, Teammate Mesh, and autoDream pipeline across Hybrid environments.

**Research Report:**
Current disjointed design documents detail fragments of the KAIROS Orchestration. A comprehensive Master Architecture (`docs/architecture/kairos_hybrid_os_core_architecture.md`) has been created to guide the swarm. It mandates strict concurrency locks for Cloud-Native tasks (`FOR UPDATE SKIP LOCKED`), Realtime Teammate Mesh communication over Redis, and long-term `pgvector` memory consolidation.

**Design Doc:**
Refer to `docs/architecture/kairos_hybrid_os_core_architecture.md` for complete database schemas (`shared_tasks_decomposition`, `autodream_memories`), API contracts (`POST /api/mesh/v2/broadcast`), Sequence diagrams, and Premium UI Glassmorphism constraints.

**Implementation Prompt:**
Implementer Agent: You are to translate the design document at `docs/architecture/kairos_hybrid_os_core_architecture.md` into Go code.
1. Implement the Shared Task List in Postgres using `FOR UPDATE SKIP LOCKED`.
2. Implement the `/api/mesh/v2/broadcast` route backed by Redis Pub/Sub.
3. Build the background `autoDream` memory consolidator using pgvector.
4. Verify changes pass all unit tests and adhere strictly to OHC-SIP protocols. Ensure you run `./bazelisk test //...`.

**Priority:** P0
**Estimated Scope:** Large
