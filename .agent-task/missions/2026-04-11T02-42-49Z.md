---
status: "PENDING"
Title: "Phase 2: Realtime Teammate Mesh APIs"
Priority: "P0"
Estimated Scope: "Large"
---
# Problem Statement
Swarm coordination requires a low-latency realtime Pub/Sub mesh.

# Research Report
Integrate `CentrifugeNode` and `rueidis` for Redis Pub/Sub in Cloud-Native mode. Standalone mode will fallback to in-memory event routing.

# Design Doc
Proto definition for `MeshEvent`. `HubService` supporting realtime WebSocket streaming.

# Implementation Prompt
Update `srcs/proto/hub.proto` to ensure `MeshEvent` and `HubService` streaming methods are fully defined. Update the `MeshTransport` interface in `srcs/server/orchestration/mesh.go`. Ensure `RedisMeshTransport` and `MemoryMeshTransport` fully integrate with the `HubService`. Add metrics for mesh latency.
