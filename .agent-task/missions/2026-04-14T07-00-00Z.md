---
status: BLOCKED
agent: Scribe
blockers: This is a backend implementation task involving Go/PostgreSQL/SQLite schemas which falls outside my explicit domain (docs/, *.md). Reassigning to an Orchestration or Implementer agent.
---
**Title:** Implement KAIROS Orchestration Features (Shared Task List, Teammate Mesh, AutoDream)

**Problem Statement**
The OHC Hybrid Agentic OS requires scalable, robust backend foundations to orchestrate a vast swarm of AI agents. We currently lack the implemented database schemas and routing logic for the Shared Task List, Teammate Mesh, and AutoDream pipelines that smoothly gracefully degrade in Standalone Desktop mode.

**Research Report**
Market analysis indicates competitors like Claude Code and Replit Agent struggle with balancing Local Privacy and Cloud Scalability. The OHC-HA architecture's primary advantage is its Hybrid RAG workflow. By using PostgreSQL with `FOR UPDATE SKIP LOCKED` combined with an SQLite fallback, we achieve unparalleled horizontal scaling and local performance.

**Design Doc**
See `docs/architecture/KAIROS_AI_OS_HYBRID_IMPLEMENTATION_DESIGN.md` for full sequence diagrams, database schemas, and architectural blueprints with the Visual Excellence Mandate applied.

Key constraints for database migrations:
- Use `TEXT PRIMARY KEY` instead of `gen_random_uuid()` or `UUID` for SQLite fallbacks.
- Use `TEXT` instead of `JSONB` for SQLite fallbacks.

**Implementation Prompt**
Hello Implementer,
Your task is to implement the KAIROS Orchestration backend features:
1. Create the `shared_tasks` table migrations in both PostgreSQL and SQLite. Use `TEXT` instead of `JSONB` and `TEXT PRIMARY KEY` for SQLite.
2. Implement the Teammate Mesh transport interface accommodating both `RedisMeshTransport` and `MemoryMeshTransport`.
3. Implement the `AutoDreamWorker` that reads `.agent-task/memory/*.yml` and stores embeddings in `autodream_memories`.
4. Ensure 100% test coverage and ensure all tests pass (`bazelisk test //...`).
5. Do not modify front-end CSS, but ensure all backend telemetry uses OpenTelemetry.

**Priority**
P0

**Estimated Scope**
Large
