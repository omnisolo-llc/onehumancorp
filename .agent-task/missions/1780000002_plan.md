1. **Mark Mission as IN_PROGRESS:** Update `.agent-task/missions/2026-04-07T08-02-24Z_hybrid_mcp_rag_sync.md` to have `status: IN_PROGRESS` and `agent: Implementer`.
2. **Database Migration:** Create a new migration file `srcs/server/db/migrations/032_hybrid_sync_metadata.sql` that adds `sync_status` and `last_sync_at` to the `autodream_memories` table. We use `autodream_memories` as it acts as the primary RAG memory context table based on its schema in `024_autodream_memories.sql` and `029_autodream_memories.sql`.
3. **Go Interface Definition:** Create `srcs/server/hub/rag_sync.go` with the `SyncStatus`, `RAGSyncRecord`, and `RAGSyncService` interfaces defined in the implementation prompt.
4. **Metrics & Observability:** Add OpenTelemetry counters `rag_records_synced_total` and `rag_sync_errors_total` to `srcs/server/telemetry/telemetry.go` and expose them so they can be registered via `telemetry.InitWithMeter`. Check memory item: *add them to `InitWithMeter(m mockableMeter)` in `telemetry.go`*. Add corresponding `Record...` functions.
5. **Verification & Tests:** Create `srcs/server/hub/rag_sync_test.go` with mock implementations of the `RAGSyncService` interface and test the basic data flow logic. Update `BUILD.bazel` files using `bazelisk run //:gazelle`.
6. **Pre-commit Steps:** Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
7. **Submit PR:** Mark the mission as `DONE` and submit the PR.
