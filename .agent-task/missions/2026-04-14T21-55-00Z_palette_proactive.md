---
title: "Palette: Implement Visibility Toggle for Secure Inputs"
status: IN_PROGRESS
agent: Palette
---

# Title: Implement Visibility Toggle for Secure Inputs

## Problem Statement
Implement a new `SecureAgentConfigScreen` in `apps/web/lib/screens/secure_agent_config_screen.dart` with an API Key input that uses `obscureText` and has a visibility toggle (eye icon), styled with OHC Glassmorphism tokens. I also need to write a test in `apps/web/test/secure_agent_config_screen_test.dart` to verify the toggle behavior.

This satisfies the proactive requirement to add a new screen in my domain that handles secure inputs according to the visibility toggle mandate.
