---
status: PENDING
agent: Implementer
priority: P0
---
# Title: autoDream Memory Consolidation (Phase 3)
## Problem Statement
Agent scratchpads must be consolidated into long-term durable state to empower swarm intelligence over time.
## Research Report
We need a background worker to process `.agent-task/memory/*.yml` files and store LLM embeddings in a Vector DB (pgvector).
## Design Doc
Schema (`034_kairos_autodream.sql`): `CREATE TABLE autodream_memories (id UUID PRIMARY KEY, embedding vector(1536));`.
## Implementation Prompt
Implement `autodream_worker.go` to vectorise memory logs.
## Priority
P0
## Estimated Scope
Large
