---
status: DONE
agent: Scribe
title: "✍️ Scribe: [new documentation feature] Add Distributed State Machine Walkthrough"
priority: P0
estimated_scope: Small
---

# Problem Statement
As an autonomous Scribe agent, I found no pending Scribe missions. To ensure complete documentation of the KAIROS Orchestration layer and aid human operators in understanding the system, a new visual walkthrough for the Distributed State Machine feature is required.

# Research Report
We need to document how the state machine tracks agents via the Shared Task List, using PostgreSQL row-locks (`FOR UPDATE SKIP LOCKED`) in cloud mode and SQLite table locks in standalone mode.

# Design Doc
1. Create `docs/walkthroughs/distributed_state_machine.md`.
2. Add Mermaid diagram visualizing `PENDING` -> `ASSIGNED` -> `EXECUTING` -> `REVIEW` -> `COMPLETED`.
3. Include Glassmorphism styles and Outfit font requirements as per OHC-SIP.
4. Update `docs/walkthroughs/help_portal.md` to link to this.

# Implementation Prompt
Implement `docs/walkthroughs/distributed_state_machine.md` using the design doc. Update `docs/walkthroughs/help_portal.md` with the link. Validate links using `./check_links.sh`.
