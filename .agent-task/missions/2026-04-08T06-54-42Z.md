---
status: "PENDING"
Title: "KAIROS Sub-Agent Orchestration Queue"
Priority: "P0"
Estimated Scope: "Large"
---

# Problem Statement
As the Swarm handles more complex workloads, we require a distributed execution framework to manage the massive concurrency of sub-tasks delegated by primary agents. This Sub-Agent Orchestration Queue must handle routing, retries, exponential backoffs, and timeouts, ensuring at-least-once delivery and resilient task execution across both Cloud-Native and Standalone modes.

# Research Report
- Based on `docs/features/kairos/sub_agent_queue.md`, the queue must seamlessly transition between different storage backends.
- **Cloud-Native Mode:** Uses Redis (via `rueidis`) Lists (`RPUSH`/`LPOP`) and Sorted Sets (ZSETs) for delayed execution, allowing for horizontal scalability across Kubernetes pods.
- **Standalone Mode:** Uses an internal SQLite table (`sub_agent_jobs`). Dequeuing relies on explicit transactions with concurrent read/write locks (simulating `FOR UPDATE SKIP LOCKED`) to prevent `SQLITE_BUSY` contention during parallel local processing.
- The queue implements robust error handling: retry mechanisms, exponential backoffs, and poison pill dead-lettering (`max_attempts` reached).

# Design Doc
**Architecture Visualization:**
```mermaid
graph TD
    subgraph KAIROS Orchestrator
        A[Task Manager] -->|Enqueue| Q{Sub-Agent Queue Interface}
    end

    Q -->|Cloud| Redis[(Redis ZSETs)]
    Q -->|Standalone| DB[(SQLite Mutexed Table)]

    Redis -->|Dequeue| W1[Worker Pod]
    DB -->|Dequeue| W2[Local Worker]

    classDef premium fill:rgba(255,255,255,0.03),stroke:rgba(255,255,255,0.08),stroke-width:1px,color:#fff,backdrop-filter:blur(20px) saturate(200%);
    class A,Q,Redis,DB,W1,W2 premium;
```

**Database Schema (SQLite Standalone `sub_agent_jobs`):**
```sql
CREATE TABLE IF NOT EXISTS sub_agent_jobs (
    id TEXT PRIMARY KEY,
    organization_id TEXT NOT NULL,
    task_type TEXT NOT NULL,
    payload TEXT NOT NULL,
    status TEXT NOT NULL DEFAULT 'QUEUED',
    attempts INTEGER DEFAULT 0,
    max_attempts INTEGER DEFAULT 3,
    run_after TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,
    created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP
);
CREATE INDEX idx_sub_agent_jobs_status_time ON sub_agent_jobs(status, run_after);
```

**Aesthetic Core (Visual Excellence Mandate):**
`<style>
body {
  backdrop-filter: blur(20px) saturate(200%);
  background: rgba(255, 255, 255, 0.03);
  font-family: 'Outfit', 'Inter', sans-serif;
}
</style>`

# Implementation Prompt
You are an Implementer agent. Your task is to build the Sub-Agent Orchestration Queue.
1. Define the `TaskQueue` interface in `srcs/server/orchestration/queue.go` with `Enqueue`, `Dequeue`, `Complete`, and `Fail` methods.
2. Implement `RedisTaskQueue` in `srcs/server/orchestration/queue_redis.go` using `rueidis`. Utilize Redis Lists for immediate tasks and ZSETs for delayed execution/backoff.
3. Implement `SQLiteTaskQueue` in `srcs/server/orchestration/queue_sqlite.go`. Use application-level mutexes to emulate `SKIP LOCKED` behavior and avoid `SQLITE_BUSY` errors.
4. Integrate the queue with the `TaskOrchestrator` to dispatch sub-tasks dynamically based on the active provider (`dbWrapper.Provider().IsSQLite()`).
5. Ensure the queue logic increments `attempts` and moves failed jobs to a dead-letter state when `attempts >= max_attempts`.
6. Write robust unit tests for both implementations in `queue_test.go`. Ensure >90% test coverage.
7. Verify functionality with `bazelisk test //srcs/server/orchestration/... --test_output=errors`.
