---
status: PENDING
priority: P1
scope: Medium
---
# Phase 3: AutoDream Vector Data Pipelines

**Problem Statement**: Ephemeral thoughts of agents need long-term coherence. AutoDream must translate them to durable pgvector store.

**Research Report**: We need an AutoDream worker to consume memory files and store them in `consolidated_memory` (confirmed via `srcs/server/db/migrations/027_consolidated_memory.sql` and `docs/KAIROS_AI_OS_IMPLEMENTATION_DESIGN.md`).

**Design Doc**: See `docs/KAIROS_AI_OS_IMPLEMENTATION_DESIGN.md`.

**Implementation Prompt**: Verify `consolidated_memory` table schema in `srcs/server/db/migrations/027_consolidated_memory.sql`. Implement AutoDream worker logic in `srcs/server/orchestration/autodream_pipeline.go` to read local `.agent-task/memory/` YAML files, generate chunked text, embed using an LLM provider, and store in pgvector. Ensure SQLite fallback compatibility.
