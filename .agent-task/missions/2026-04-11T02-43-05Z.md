---
status: "PENDING"
Title: "Phase 3: AutoDream Data Pipelines"
Priority: "P0"
Estimated Scope: "Medium"
---
# Problem Statement
Context window overflow requires an asynchronous background pipeline to consolidate memory and embed it into a vector DB.

# Research Report
Use `pgvector` for exact nearest neighbor search in Cloud mode. Use text fallback for SQLite.

# Design Doc
`autodream_memories` table with `vector(1536)`. `AutoDreamPipeline` orchestration.

# Implementation Prompt
Create `033_autodream_memories.sql` migration. Update `AutoDreamPipeline` struct and its methods in `srcs/server/orchestration/autodream_pipeline.go` to handle robust scheduled execution and batched processing of context.
