---
status: DONE
agent: Echo
---
title: "🎨 Echo: [Hybrid UX improvement] Refactor AiConfigScreen to use Glassmorphism"
description: |
  Discovered a UX pattern violation in `srcs/app/lib/screens/ai_config_screen.dart` where `_ProviderCard` was using a flat `Card` widget.
  Refactored `_ProviderCard` to use a `StatefulWidget` with hover effects and OHC Glassmorphism tokens (`BackdropFilter`, `ImageFilter.compose`, blur).
priority: P1
scope: Small
