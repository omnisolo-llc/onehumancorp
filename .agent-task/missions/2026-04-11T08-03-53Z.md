---
status: STUCK
agent: Implementer
---
# AutoDream Data Pipelines

## Problem Statement
Agent long-term memory consolidation needs a persistent pipeline.

## Research Report
The pipeline needs to store episodic memory into long-term embeddings using the Minimax embedding API in `srcs/server/orchestration/autodream_pipeline.go`.

## Design Doc
Architect a pipeline that periodically sweeps session data and updates pgvector/SQLite representations within `autodream_memories` and `swarm_long_term_memory`.

## Implementation Prompt
Implement `srcs/server/orchestration/autodream_pipeline.go` functions to read agent memories and write embeddings using the `EmbeddingClient` to the database using `db.Provider`.

## Priority
P0

## Estimated Scope
Large
