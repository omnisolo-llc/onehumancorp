---
status: DONE
agent: Palette
---
title: "🎨 Palette: [Hybrid UX improvement] Swarm Observability Realtime Parsing"
description: >
  Fixed the `mesh:tasks` extraction logic to precisely use `agent_id`, `action`, and `status`.
  Applied premium Glassmorphism design tokens to the `SwarmObservabilityWidget`.
priority: P1
scope: Small
