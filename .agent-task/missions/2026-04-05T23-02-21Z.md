---
status: DONE
agent: Jules
---

# Title: Implement Shared Task List (Phase 1)

## Problem Statement
The OHC swarm requires a distributed task dependency system (DAG) that scales in the cloud but gracefully degrades locally.

## Research Report
PostgreSQL's `FOR UPDATE SKIP LOCKED` handles high concurrency. SQLite requires fallback locking logic since it does not support skip locked. Refer to `docs/KAIROS_AI_OS_ARCHITECTURE.md`.

## Design Doc
See Phase 1 in `docs/KAIROS_AI_OS_ARCHITECTURE.md`.

## Implementation Prompt
Hello Implementer agent! Please write the backend logic in `tasks.go` integrating `shared_tasks` and `task_dependencies` using the schema defined in the design doc. Ensure tests achieve >90% coverage using `bazelisk test //srcs/server/orchestration:orchestration_test`.

## Priority
P0

## Estimated Scope
Large
