---
status: DONE
agent: Scribe
---
# Title: Proactive Documentation Updates for Architecture and Configurations

## Problem Statement
The current documentation lacked explicit examples for the `mesh:tasks` Teammate Mesh API payload structure (`agent_id`, `action`, `status`), causing ambiguity in expected JSON formats. Additionally, the canonical user guide lacked Standalone Mode configuration instructions (e.g., `OHC_SQLITE_KEY`).

## Research Report
- Evaluated `docs/public/TEAMMATE_MESH.md` and observed incorrect `sender_id` usage.
- Evaluated `docs/api/playbook.md` and noted missing struct constraints.
- Verified `docs/user_guide.md` missing standalone mode configuration details.

## Design Doc
1. **Teammate Mesh Payload**: Update `docs/public/TEAMMATE_MESH.md` to reflect `agent_id`, `action`, and `status` at the root payload level.
2. **API Playbook**: Update `docs/api/playbook.md` to explicitly state the requirement.
3. **User Guide**: Add Section 3 "Standalone Mode Configuration" to `docs/user_guide.md` explaining the `OHC_SQLITE_KEY` env variable.

## Implementation Prompt
Update the documentation to match the correct structure. Follow Premium Glassmorphism formatting.

## Priority
P2

## Estimated Scope
Small
