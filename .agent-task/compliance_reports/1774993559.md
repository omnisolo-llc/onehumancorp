# OHC Compliance Report
**Date:** Tue Mar 31 21:48:56 UTC 2026

## NPM Packages
- **Tool**: `pnpm audit`
- **Finding**: High-severity vulnerabilities found in `@modelcontextprotocol/sdk` (ReDoS and DNS rebinding) introduced via `@modelcontextprotocol/server-github`.
- **Action**: Modified `package.json` to enforce `overrides` for `@modelcontextprotocol/sdk: >=1.25.2`. Verified with `pnpm install` and subsequent `pnpm audit` (0 vulnerabilities found).

## Go Packages
- **Tool**: `govulncheck`
- **Finding**: Outdated packages missing go version compatibility and older crypto packages.
- **Action**: Updated `golang.org/x/crypto` to v0.49.0 and refreshed module dependencies (`go mod tidy`). Verified 0 vulnerabilities via `govulncheck`.

## Core Values Alignment
- The removal of known vulnerabilities aligns with the *Absolute Autonomy* and Swarm Security mandates, protecting the One Human Corp Swarm from external injection vectors and supply chain attacks.
