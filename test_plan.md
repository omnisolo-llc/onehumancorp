1. Add controllers `controller: _endpointUrlController` and `controller: _tokenLimitController` to `TextField`s in `srcs/app/lib/screens/agent_hire_wizard_screen.dart`.
2. Add dispose method for `_endpointUrlController` and `_tokenLimitController`.
