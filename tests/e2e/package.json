{
  "name": "ohc-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end Playwright tests for the One Human Corp web service",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.1",
    "@types/node": "^20.0.0",
    "typescript": "^6.0.3"
  }
}
