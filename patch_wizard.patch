--- src/app/wizard.slint
+++ src/app/wizard.slint
@@ -2,6 +2,7 @@
 import { GlassCard } from "glass_card.slint";
+import { AdvancedToggle } from "advanced_toggle.slint";

 export component Wizard inherits Window {
     min-width: 375px;
     preferred-width: 500px;
@@ -10,6 +11,8 @@

     in-out property <int> step: 0;

+    in-out property <bool> is_advanced: false;
+
     in property <string> issue_title: "Helper Needs Connection";
     in property <string> issue_description: "Your Customer Support helper is having trouble connecting to the email service. Let's get this fixed so they can get back to work.";

@@ -19,6 +22,9 @@
     callback prev_step();
     callback resolve_issue();
+    callback toggle_advanced();
+
+    toggle_advanced => { is_advanced = !is_advanced; }

     next_step => { step = step + 1; }
     prev_step => { step = step - 1; }
@@ -31,6 +37,10 @@
         HorizontalBox {
             Text { text: "Help Me Fix This"; font-size: 24px; font-family: "Outfit"; font-weight: 700; color: white; }
+            AdvancedToggle {
+                is_advanced <=> root.is_advanced;
+                toggled => { toggle_advanced(); }
+            }
         }

         HorizontalBox {
@@ -100,6 +110,13 @@
                     Text { text: "We need your permission to securely refresh the connection to your email provider. This takes one click and requires no technical knowledge."; font-family: "Inter"; font-size: 14px; color: #cbd5e1; wrap: word-wrap; }
                 }
             }
+            if (is_advanced) : GlassCard {
+                background: rgba(0, 0, 0, 0.5);
+                VerticalBox {
+                    padding: 16px;
+                    Text { text: "CLI Command: \\n$ ohc-cli integrations reconnect --helper \"Customer Support\" --provider \"Email\""; font-family: "Inter"; font-size: 12px; color: #a855f7; wrap: word-wrap; }
+                }
+            }
         }

         // Step 2: Resolve & Apply
@@ -115,6 +132,13 @@
                     Text { text: "Click the button below to securely apply the fix. Your helper will be back to work in seconds."; font-family: "Inter"; font-size: 14px; color: #cbd5e1; wrap: word-wrap; }
                 }
             }
+            if (is_advanced) : GlassCard {
+                background: rgba(0, 0, 0, 0.5);
+                VerticalBox {
+                    padding: 16px;
+                    Text { text: "Raw Configuration Delta: \\n{ \"email_integration\": { \"status\": \"active\", \"retries\": 0 } }"; font-family: "Inter"; font-size: 12px; color: #a855f7; wrap: word-wrap; }
+                }
+            }
         }

         Rectangle {} // Spacer
