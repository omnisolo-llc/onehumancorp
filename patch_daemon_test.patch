--- src/server/orchestration/hybrid_sync/daemon_test.rs
+++ src/server/orchestration/hybrid_sync/daemon_test.rs
@@ -42,14 +42,10 @@
         .unwrap();

         sqlx::query(
-            "CREATE TABLE IF NOT EXISTS sub_agent_queue (
+            "CREATE TABLE IF NOT EXISTS agent_missions (
                 id VARCHAR PRIMARY KEY,
                 tenant_id VARCHAR NOT NULL,
-                parent_task_id VARCHAR,
                 payload TEXT,
                 status VARCHAR,
-                worker_id VARCHAR,
-                scheduled_at TIMESTAMP,
-                completed_at TIMESTAMP,
                 created_at TIMESTAMP,
                 updated_at TIMESTAMP
             )"
@@ -80,7 +76,7 @@
         assert_eq!(status, "SYNCED");

         // Let's also check the pg queue redaction.
-        let queue_row = sqlx::query("SELECT payload FROM sub_agent_queue WHERE payload LIKE '%test_mem_1%'")
+        let queue_row = sqlx::query("SELECT payload FROM agent_missions WHERE payload LIKE '%test_mem_1%'")
             .fetch_one(&pg_pool)
             .await
             .unwrap();
