--- src/server/queue.rs
+++ src/server/queue.rs
@@ -802,6 +802,7 @@
     #[tokio::test]
     async fn test_task_queue_service_push_claim() {
         let db_builder = crate::db::DB::new().await;
         if db_builder.is_err() {
             return;
@@ -812,7 +813,7 @@
         match &db.store {
             DbStore::Postgres => {
             let pool = sqlx::postgres::PgPoolOptions::new()
-                .before_acquire(|conn, _meta| {
+                .acquire_timeout(std::time::Duration::from_millis(500)).before_acquire(|conn, _meta| {
                     Box::pin(async move {
                         use sqlx::Executor;
                         conn.execute("SELECT set_config('app.current_tenant', 'system', false)").await?;
                         Ok(true)
                     })
                 })
                 .connect_lazy("postgres://postgres:postgres@localhost:5432/ohc").unwrap();
-            if !matches!(tokio::time::timeout(std::time::Duration::from_millis(500), sqlx::query("SELECT 1").execute(&pool)).await, Ok(Ok(_))) { return; }
+            if !matches!(tokio::time::timeout(std::time::Duration::from_millis(100), sqlx::query("SELECT 1").execute(&pool)).await, Ok(Ok(_))) { return; }

                 sqlx::query("CREATE TABLE IF NOT EXISTS shared_tasks (id TEXT PRIMARY KEY, organization_id TEXT NOT NULL, parent_plan_id TEXT, dependencies JSONB, title TEXT, description TEXT, status TEXT, priority TEXT, payload JSONB, locked_until TIMESTAMPTZ, ultraplan_phase TEXT, auto_dreamed BOOLEAN DEFAULT FALSE, created_at TIMESTAMPTZ, updated_at TIMESTAMPTZ)")
                     .execute(&pool)
@@ -856,6 +857,7 @@
     #[tokio::test]
     async fn test_task_queue_service_with_dependencies() {
         let db_builder = crate::db::DB::new().await;
         if db_builder.is_err() {
             return;
@@ -866,7 +868,7 @@
         match &db.store {
             DbStore::Postgres => {
             let pool = sqlx::postgres::PgPoolOptions::new()
-                .before_acquire(|conn, _meta| {
+                .acquire_timeout(std::time::Duration::from_millis(500)).before_acquire(|conn, _meta| {
                     Box::pin(async move {
                         use sqlx::Executor;
                         conn.execute("SELECT set_config('app.current_tenant', 'system', false)").await?;
                         Ok(true)
                     })
                 })
                 .connect_lazy("postgres://postgres:postgres@localhost:5432/ohc").unwrap();
-            if !matches!(tokio::time::timeout(std::time::Duration::from_millis(500), sqlx::query("SELECT 1").execute(&pool)).await, Ok(Ok(_))) { return; }
+            if !matches!(tokio::time::timeout(std::time::Duration::from_millis(100), sqlx::query("SELECT 1").execute(&pool)).await, Ok(Ok(_))) { return; }

                 sqlx::query("CREATE TABLE IF NOT EXISTS shared_tasks (id TEXT PRIMARY KEY, organization_id TEXT NOT NULL, parent_plan_id TEXT, dependencies JSONB, title TEXT, description TEXT, status TEXT, priority TEXT, payload JSONB, locked_until TIMESTAMPTZ, ultraplan_phase TEXT, auto_dreamed BOOLEAN DEFAULT FALSE, created_at TIMESTAMPTZ, updated_at TIMESTAMPTZ)")
                     .execute(&pool)
