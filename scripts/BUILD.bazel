load("@rules_shell//shell:sh_test.bzl", "sh_test")

exports_files([
    "run-playwright.mjs",
])

sh_test(
    name = "lint_pii",
    srcs = ["lint/lint_pii.sh"],
    # Note: data attribute removed to avoid referencing ignored srcs dir directly,
    # relying on the sandbox's layout or executing without deps if not strictly sandboxed in a way that breaks find.
    size = "small",
)
