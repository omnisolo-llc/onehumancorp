# Maintenance Helpers

Maintenance helpers that are not part of the supported build, test, or runtime entrypoints live here.

Use these scripts as one-off repair or migration utilities; keep user-facing commands such as top-level test and run wrappers at the repository root.