1.  **Define the Universal Protocol:** I will update the `src/proto/hub.proto` file to include standard message types for the bridge: `MeshEvent` with proper routing headers (`OriginOrgID`, `DestinationOrgID`, `Mode`: Standalone/Cloud, etc.). I will verify the edit using `git diff src/proto/hub.proto`.
2.  **Implement `UniversalTransportBridge`:** I will create a `UniversalTransportBridge` struct in `src/server/orchestration/universal_bridge.go` containing the local bus (`msgbus.Bus`) and external WebSocket connection (`*websocket.Conn`), and implement a `NewUniversalTransportBridge` constructor and a `Start(ctx context.Context)` method. I will verify the creation by reading the file with `cat src/server/orchestration/universal_bridge.go`.
3.  **Cross-Mode State Handoff:** I will add methods `SyncPendingTasks`, `SyncContext` and `HandleModeSwitch` to the bridge in `src/server/orchestration/universal_bridge.go` to handle state synchronization when switching modes (e.g., syncing pending tasks from SQLite to Cloud, and fetching updates from Cloud to SQLite). I will verify this by reading the file with `cat src/server/orchestration/universal_bridge.go`.
4.  **Distributed Locking Bridge:** I will extend the `resilience/lock.go` and `orchestration/mesh.go` distributed locking mechanisms to coordinate locks across the bridge. Specifically, I will add `AcquireCrossModeLock` and `ReleaseCrossModeLock` in `src/server/lib/resilience/distributed_locks.go` and update `src/server/orchestration/mesh/mesh.go`. I will verify the edit using `git diff`.
5.  **Documentation:** I will complete `docs/architecture/universal_transport_bridge.md` outlining the design. I will verify the edit using `cat docs/architecture/universal_transport_bridge.md`.
6.  **Testing:** I will write tests in `src/server/orchestration/universal_bridge_test.go` covering the `SyncPendingTasks`, `SyncContext`, and `HandleModeSwitch` methods. I will verify the file creation using `cat src/server/orchestration/universal_bridge_test.go`.
7.  Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
8.  **Run Tests:** I will run `bazelisk test //...` to ensure all tests pass.
