🧙 Wizard: [Day One Business Setup Wizard]

**What**
- Added `lib/features/onboarding/business_setup_wizard.dart` containing the initial Day One stepper UI for business and goal setup.
- Implemented state persistence via backend integration with `/api/provision`, including a new GET endpoint in `services/onboarding/server.go` to support resuming state on app init.
- Fixed a `RenderFlex` overflow bug in the existing `AiAgentConfigWizard` UI.
- Implemented OHC UI/UX guidelines: Glassmorphism, specific typography (`Outfit`/`Inter`), SVG icons on options, and tooltips.
- Added a basic E2E Flutter test `business_setup_wizard_test.dart` to verify state transitions and form validation.

**Why**
To provide an intuitive, zero-technical-knowledge-required onboarding experience for new users provisioning the OHC hybrid agentic OS, per the Principal UX Wizard mission.
