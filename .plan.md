1. **Mark Mission as IN_PROGRESS:** Update the `.agent-task/missions/2026-04-07T08-02-24Z_hybrid_mcp_rag_sync.md` file status to `IN_PROGRESS` and agent to `jules`.
2. **Database Migration:** Create `srcs/server/db/migrations/032_add_hybrid_sync_metadata.sql` that adds `sync_status` and `last_sync_at` to the `swarm_memory_embeddings` table. Since we don't have a `rag_memories` table, `swarm_memory_embeddings` is used for context table based on memory context.
3. **Go Interface Definition & Telemetry:** Create `srcs/server/hub/rag_sync.go` defining the interface, struct and adding telemetry for sync process as detailed in the mission. Create telemetry integration to make sure telemetry variables are accessible and setup correctly.
4. **Unit Tests:** Create `srcs/server/hub/rag_sync_test.go` to mock interface and verify data flow logic.
5. **Update Build:** Run `bazelisk run //:gazelle` to generate `BUILD.bazel` for the new `hub` package.
6. **Testing:** Run `bazelisk test //srcs/server/...` to verify everything works.
7. **Mark Mission as DONE:** Update the mission markdown file status to `DONE`.
8. **Pre-commit:** Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
9. **Submit:** Commit the changes and submit the PR.
