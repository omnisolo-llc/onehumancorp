load("@rules_go//go:def.bzl", "go_library")

# Add a dummy BUILD.bazel to satisfy bazel if needed or just don't run tests on it.

go_library(
    name = "monitoring",
    srcs = ["metrics.go"],
    importpath = "github.com/onehumancorp/mono/src/server/monitoring",
    visibility = ["//visibility:public"],
    deps = ["@io_opentelemetry_go_otel//attribute"],
)
