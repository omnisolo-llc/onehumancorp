--- service.rs
+++ service.rs
@@ -176,7 +176,7 @@
                 }

                 let q = if mobile_optimized {
-                    "SELECT id, tenant_id, total_amount, status FROM orders WHERE tenant_id = $1 LIMIT 50"
+                    "SELECT id, '' as tenant_id, total_amount, status FROM orders WHERE tenant_id = $1 LIMIT 50"
                 } else {
                     "SELECT id, tenant_id, total_amount, status FROM orders WHERE tenant_id = $1 LIMIT 50"
                 };
