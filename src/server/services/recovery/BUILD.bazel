load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "recovery",
    srcs = [
        "mod.rs",
        "service.rs",
    ],
    crate_root = "mod.rs",
    visibility = ["//visibility:public"],
    deps = [
        "@crates//:serde",
        "@crates//:serde_json",
        "@crates//:uuid",
    ],
)
