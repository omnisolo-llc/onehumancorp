#[cfg(test)]
mod tests {
    use super::super::{api::AppState, service::OmnichannelService, Inbox, ChannelConnection, Contact, Conversation, Message};

    #[test]
    fn test_dummy() {
        assert!(true);
    }
}
