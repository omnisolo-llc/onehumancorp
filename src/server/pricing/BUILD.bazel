load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

rust_library(
    name = "server_pricing",
    srcs = [
        "budget.rs",
        "cache.rs",
        "calculator.rs",
        "compression.rs",
        "mod.rs",
        "prompt_caching.rs",
        "rate_limit.rs",
    ],
    crate_root = "mod.rs",
    edition = "2024",
    visibility = ["//visibility:public"],
    deps = [
        "//src/server/harness:server_harness",
        "@crates//:chrono",
        "@crates//:redis",
        "@crates//:serde",
        "@crates//:serde_json",
        "@crates//:tokio",
        "@crates//:tracing",
        "@crates//:flate2",
        "@crates//:base64",
        "@crates//:dashmap",
        "@crates//:sha2",
        "@crates//:opentelemetry",
        "@crates//:hex",
        "@crates//:image",
    ],
    proc_macro_deps = [
        "@crates//:async-trait",
    ],
)

rust_test(
    name = "server_pricing_unit_test",
    size = "small",
    crate = ":server_pricing",
    edition = "2024",
    target_compatible_with = select({
        "@platforms//os:linux": [],
        "//conditions:default": ["@platforms//:incompatible"],
    }),
)
