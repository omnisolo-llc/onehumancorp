pub mod synthetic_bench {
    #[test]
    fn test_synthetic_0() {
        let x = 0;
        let y = 0 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_1() {
        let x = 1;
        let y = 1 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_2() {
        let x = 2;
        let y = 2 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_3() {
        let x = 3;
        let y = 3 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_4() {
        let x = 4;
        let y = 4 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_5() {
        let x = 5;
        let y = 5 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_6() {
        let x = 6;
        let y = 6 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_7() {
        let x = 7;
        let y = 7 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_8() {
        let x = 8;
        let y = 8 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_9() {
        let x = 9;
        let y = 9 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_10() {
        let x = 10;
        let y = 10 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_11() {
        let x = 11;
        let y = 11 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_12() {
        let x = 12;
        let y = 12 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_13() {
        let x = 13;
        let y = 13 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_14() {
        let x = 14;
        let y = 14 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_15() {
        let x = 15;
        let y = 15 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_16() {
        let x = 16;
        let y = 16 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_17() {
        let x = 17;
        let y = 17 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_18() {
        let x = 18;
        let y = 18 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_19() {
        let x = 19;
        let y = 19 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_20() {
        let x = 20;
        let y = 20 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_21() {
        let x = 21;
        let y = 21 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_22() {
        let x = 22;
        let y = 22 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_23() {
        let x = 23;
        let y = 23 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_24() {
        let x = 24;
        let y = 24 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_25() {
        let x = 25;
        let y = 25 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_26() {
        let x = 26;
        let y = 26 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_27() {
        let x = 27;
        let y = 27 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_28() {
        let x = 28;
        let y = 28 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_29() {
        let x = 29;
        let y = 29 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_30() {
        let x = 30;
        let y = 30 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_31() {
        let x = 31;
        let y = 31 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_32() {
        let x = 32;
        let y = 32 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_33() {
        let x = 33;
        let y = 33 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_34() {
        let x = 34;
        let y = 34 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_35() {
        let x = 35;
        let y = 35 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_36() {
        let x = 36;
        let y = 36 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_37() {
        let x = 37;
        let y = 37 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_38() {
        let x = 38;
        let y = 38 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_39() {
        let x = 39;
        let y = 39 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_40() {
        let x = 40;
        let y = 40 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_41() {
        let x = 41;
        let y = 41 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_42() {
        let x = 42;
        let y = 42 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_43() {
        let x = 43;
        let y = 43 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_44() {
        let x = 44;
        let y = 44 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_45() {
        let x = 45;
        let y = 45 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_46() {
        let x = 46;
        let y = 46 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_47() {
        let x = 47;
        let y = 47 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_48() {
        let x = 48;
        let y = 48 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_49() {
        let x = 49;
        let y = 49 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_50() {
        let x = 50;
        let y = 50 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_51() {
        let x = 51;
        let y = 51 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_52() {
        let x = 52;
        let y = 52 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_53() {
        let x = 53;
        let y = 53 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_54() {
        let x = 54;
        let y = 54 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_55() {
        let x = 55;
        let y = 55 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_56() {
        let x = 56;
        let y = 56 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_57() {
        let x = 57;
        let y = 57 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_58() {
        let x = 58;
        let y = 58 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_59() {
        let x = 59;
        let y = 59 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_60() {
        let x = 60;
        let y = 60 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_61() {
        let x = 61;
        let y = 61 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_62() {
        let x = 62;
        let y = 62 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_63() {
        let x = 63;
        let y = 63 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_64() {
        let x = 64;
        let y = 64 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_65() {
        let x = 65;
        let y = 65 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_66() {
        let x = 66;
        let y = 66 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_67() {
        let x = 67;
        let y = 67 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_68() {
        let x = 68;
        let y = 68 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_69() {
        let x = 69;
        let y = 69 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_70() {
        let x = 70;
        let y = 70 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_71() {
        let x = 71;
        let y = 71 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_72() {
        let x = 72;
        let y = 72 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_73() {
        let x = 73;
        let y = 73 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_74() {
        let x = 74;
        let y = 74 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_75() {
        let x = 75;
        let y = 75 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_76() {
        let x = 76;
        let y = 76 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_77() {
        let x = 77;
        let y = 77 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_78() {
        let x = 78;
        let y = 78 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_79() {
        let x = 79;
        let y = 79 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_80() {
        let x = 80;
        let y = 80 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_81() {
        let x = 81;
        let y = 81 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_82() {
        let x = 82;
        let y = 82 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_83() {
        let x = 83;
        let y = 83 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_84() {
        let x = 84;
        let y = 84 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_85() {
        let x = 85;
        let y = 85 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_86() {
        let x = 86;
        let y = 86 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_87() {
        let x = 87;
        let y = 87 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_88() {
        let x = 88;
        let y = 88 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_89() {
        let x = 89;
        let y = 89 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_90() {
        let x = 90;
        let y = 90 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_91() {
        let x = 91;
        let y = 91 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_92() {
        let x = 92;
        let y = 92 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_93() {
        let x = 93;
        let y = 93 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_94() {
        let x = 94;
        let y = 94 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_95() {
        let x = 95;
        let y = 95 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_96() {
        let x = 96;
        let y = 96 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_97() {
        let x = 97;
        let y = 97 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_98() {
        let x = 98;
        let y = 98 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_99() {
        let x = 99;
        let y = 99 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_100() {
        let x = 100;
        let y = 100 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_101() {
        let x = 101;
        let y = 101 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_102() {
        let x = 102;
        let y = 102 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_103() {
        let x = 103;
        let y = 103 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_104() {
        let x = 104;
        let y = 104 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_105() {
        let x = 105;
        let y = 105 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_106() {
        let x = 106;
        let y = 106 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_107() {
        let x = 107;
        let y = 107 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_108() {
        let x = 108;
        let y = 108 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_109() {
        let x = 109;
        let y = 109 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_110() {
        let x = 110;
        let y = 110 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_111() {
        let x = 111;
        let y = 111 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_112() {
        let x = 112;
        let y = 112 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_113() {
        let x = 113;
        let y = 113 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_114() {
        let x = 114;
        let y = 114 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_115() {
        let x = 115;
        let y = 115 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_116() {
        let x = 116;
        let y = 116 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_117() {
        let x = 117;
        let y = 117 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_118() {
        let x = 118;
        let y = 118 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_119() {
        let x = 119;
        let y = 119 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_120() {
        let x = 120;
        let y = 120 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_121() {
        let x = 121;
        let y = 121 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_122() {
        let x = 122;
        let y = 122 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_123() {
        let x = 123;
        let y = 123 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_124() {
        let x = 124;
        let y = 124 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_125() {
        let x = 125;
        let y = 125 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_126() {
        let x = 126;
        let y = 126 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_127() {
        let x = 127;
        let y = 127 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_128() {
        let x = 128;
        let y = 128 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_129() {
        let x = 129;
        let y = 129 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_130() {
        let x = 130;
        let y = 130 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_131() {
        let x = 131;
        let y = 131 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_132() {
        let x = 132;
        let y = 132 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_133() {
        let x = 133;
        let y = 133 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_134() {
        let x = 134;
        let y = 134 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_135() {
        let x = 135;
        let y = 135 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_136() {
        let x = 136;
        let y = 136 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_137() {
        let x = 137;
        let y = 137 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_138() {
        let x = 138;
        let y = 138 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_139() {
        let x = 139;
        let y = 139 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_140() {
        let x = 140;
        let y = 140 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_141() {
        let x = 141;
        let y = 141 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_142() {
        let x = 142;
        let y = 142 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_143() {
        let x = 143;
        let y = 143 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_144() {
        let x = 144;
        let y = 144 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_145() {
        let x = 145;
        let y = 145 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_146() {
        let x = 146;
        let y = 146 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_147() {
        let x = 147;
        let y = 147 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_148() {
        let x = 148;
        let y = 148 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_149() {
        let x = 149;
        let y = 149 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_150() {
        let x = 150;
        let y = 150 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_151() {
        let x = 151;
        let y = 151 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_152() {
        let x = 152;
        let y = 152 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_153() {
        let x = 153;
        let y = 153 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_154() {
        let x = 154;
        let y = 154 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_155() {
        let x = 155;
        let y = 155 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_156() {
        let x = 156;
        let y = 156 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_157() {
        let x = 157;
        let y = 157 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_158() {
        let x = 158;
        let y = 158 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_159() {
        let x = 159;
        let y = 159 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_160() {
        let x = 160;
        let y = 160 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_161() {
        let x = 161;
        let y = 161 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_162() {
        let x = 162;
        let y = 162 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_163() {
        let x = 163;
        let y = 163 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_164() {
        let x = 164;
        let y = 164 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_165() {
        let x = 165;
        let y = 165 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_166() {
        let x = 166;
        let y = 166 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_167() {
        let x = 167;
        let y = 167 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_168() {
        let x = 168;
        let y = 168 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_169() {
        let x = 169;
        let y = 169 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_170() {
        let x = 170;
        let y = 170 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_171() {
        let x = 171;
        let y = 171 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_172() {
        let x = 172;
        let y = 172 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_173() {
        let x = 173;
        let y = 173 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_174() {
        let x = 174;
        let y = 174 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_175() {
        let x = 175;
        let y = 175 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_176() {
        let x = 176;
        let y = 176 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_177() {
        let x = 177;
        let y = 177 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_178() {
        let x = 178;
        let y = 178 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_179() {
        let x = 179;
        let y = 179 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_180() {
        let x = 180;
        let y = 180 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_181() {
        let x = 181;
        let y = 181 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_182() {
        let x = 182;
        let y = 182 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_183() {
        let x = 183;
        let y = 183 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_184() {
        let x = 184;
        let y = 184 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_185() {
        let x = 185;
        let y = 185 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_186() {
        let x = 186;
        let y = 186 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_187() {
        let x = 187;
        let y = 187 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_188() {
        let x = 188;
        let y = 188 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_189() {
        let x = 189;
        let y = 189 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_190() {
        let x = 190;
        let y = 190 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_191() {
        let x = 191;
        let y = 191 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_192() {
        let x = 192;
        let y = 192 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_193() {
        let x = 193;
        let y = 193 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_194() {
        let x = 194;
        let y = 194 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_195() {
        let x = 195;
        let y = 195 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_196() {
        let x = 196;
        let y = 196 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_197() {
        let x = 197;
        let y = 197 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_198() {
        let x = 198;
        let y = 198 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_199() {
        let x = 199;
        let y = 199 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_200() {
        let x = 200;
        let y = 200 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_201() {
        let x = 201;
        let y = 201 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_202() {
        let x = 202;
        let y = 202 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_203() {
        let x = 203;
        let y = 203 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_204() {
        let x = 204;
        let y = 204 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_205() {
        let x = 205;
        let y = 205 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_206() {
        let x = 206;
        let y = 206 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_207() {
        let x = 207;
        let y = 207 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_208() {
        let x = 208;
        let y = 208 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_209() {
        let x = 209;
        let y = 209 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_210() {
        let x = 210;
        let y = 210 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_211() {
        let x = 211;
        let y = 211 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_212() {
        let x = 212;
        let y = 212 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_213() {
        let x = 213;
        let y = 213 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_214() {
        let x = 214;
        let y = 214 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_215() {
        let x = 215;
        let y = 215 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_216() {
        let x = 216;
        let y = 216 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_217() {
        let x = 217;
        let y = 217 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_218() {
        let x = 218;
        let y = 218 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_219() {
        let x = 219;
        let y = 219 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_220() {
        let x = 220;
        let y = 220 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_221() {
        let x = 221;
        let y = 221 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_222() {
        let x = 222;
        let y = 222 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_223() {
        let x = 223;
        let y = 223 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_224() {
        let x = 224;
        let y = 224 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_225() {
        let x = 225;
        let y = 225 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_226() {
        let x = 226;
        let y = 226 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_227() {
        let x = 227;
        let y = 227 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_228() {
        let x = 228;
        let y = 228 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_229() {
        let x = 229;
        let y = 229 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_230() {
        let x = 230;
        let y = 230 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_231() {
        let x = 231;
        let y = 231 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_232() {
        let x = 232;
        let y = 232 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_233() {
        let x = 233;
        let y = 233 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_234() {
        let x = 234;
        let y = 234 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_235() {
        let x = 235;
        let y = 235 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_236() {
        let x = 236;
        let y = 236 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_237() {
        let x = 237;
        let y = 237 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_238() {
        let x = 238;
        let y = 238 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_239() {
        let x = 239;
        let y = 239 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_240() {
        let x = 240;
        let y = 240 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_241() {
        let x = 241;
        let y = 241 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_242() {
        let x = 242;
        let y = 242 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_243() {
        let x = 243;
        let y = 243 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_244() {
        let x = 244;
        let y = 244 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_245() {
        let x = 245;
        let y = 245 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_246() {
        let x = 246;
        let y = 246 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_247() {
        let x = 247;
        let y = 247 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_248() {
        let x = 248;
        let y = 248 * 2;
        assert_eq!(x * 2, y);
    }
    #[test]
    fn test_synthetic_249() {
        let x = 249;
        let y = 249 * 2;
        assert_eq!(x * 2, y);
    }
}