load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

rust_library(
    name = "omnichannel_ingress",
    srcs = glob(["src/**/*.rs"]),
    visibility = ["//visibility:public"],
    deps = [
        "@crates//:serde",
        "@crates//:serde_json",
        "@crates//:axum",
        "@crates//:tokio",
    ],
)

rust_test(
    name = "omnichannel_ingress_test",
    crate = ":omnichannel_ingress",
)
