[package]
name = "server_integrations_printful"
version = "0.1.0"
edition = "2024"

[lib]
path = "mod.rs"

[dependencies]
server_integrations_core = { path = "../core" }
tokio = { version = "1.28", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
reqwest = { version = "0.12.3", features = ["json"] }
