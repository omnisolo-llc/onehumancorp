load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

rust_library(
    name = "chat",
    srcs = glob(["**/*.rs"]),
    visibility = ["//visibility:public"],
    deps = [
        "//src/server/common",
        "@crates//:axum",
        "@crates//:serde",
        "@crates//:serde_json",
        "@crates//:sqlx",
        "@crates//:tokio",
        "@crates//:uuid",
    ],
)
