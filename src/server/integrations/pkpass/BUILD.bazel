load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

rust_library(
    name = "server_integrations_pkpass",
    srcs = glob(["src/**/*.rs"]),
    visibility = ["//visibility:public"],
    deps = [
        "//src/server/common:server_common",
        "@crates//:tracing",
        "@crates//:serde",
        "@crates//:serde_json",
        "@crates//:sha1",
        "@crates//:zip",
    ],
)

rust_test(
    name = "server_integrations_pkpass_test",
    srcs = glob(["tests/**/*.rs"]),
    deps = [
        ":server_integrations_pkpass",
    ],
)
