load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

rust_library(
    name = "server_integrations_whatsapp",
    srcs = [
        "bazel_lib.rs",
        "client.rs",
        "provider.rs",
    ],
    crate_name = "server_integrations_whatsapp",
    crate_root = "bazel_lib.rs",
    visibility = ["//visibility:public"],
    deps = [
        "//src/server/integrations/core:server_integrations_core",
        "@crates//:reqwest",
        "@crates//:serde_json",
        "@crates//:tokio",
        "@crates//:uuid",
    ],
    proc_macro_deps = [
        "@crates//:async-trait",
    ],
)

exports_files([
    "mod.rs",
    "client.rs",
    "provider.rs",
])
