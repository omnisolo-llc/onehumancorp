[package]
name = "server_integrations_task_scheduler"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1"
tokio = { workspace = true }
uuid = { version = "1", features = ["v4", "serde"] }

[dev-dependencies]
temp-env = "0.3.6"

[lib]
path = "mod.rs"
