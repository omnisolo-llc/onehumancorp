[package]
name = "server_integrations_restic"
version = "0.1.0"
edition = "2024"

[dependencies]
server_integrations_core = { path = "../core" }

[dev-dependencies]
temp-env = "0.3.6"

[lib]
path = "mod.rs"
