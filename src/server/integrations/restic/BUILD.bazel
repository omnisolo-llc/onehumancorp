exports_files(["Cargo.toml"])

load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

rust_library(
    name = "server_integrations_restic",
    srcs = [
        "mod.rs",
        "provider.rs",
        "provider_test.rs",
    ],
    crate_root = "mod.rs",
    edition = "2024",
    visibility = ["//visibility:public"],
    deps = [
        "//src/server/integrations/core:server_integrations_core",
        "@crates//:async-trait",
        "@crates//:reqwest",
        "@crates//:serde",
        "@crates//:serde_json",
        "@crates//:sqlx",
        "@crates//:tokio",
        "@crates//:tracing",
        "@crates//:uuid",
    ],
)

rust_test(
    name = "server_integrations_restic_unit_test",
    size = "small",
    crate = ":server_integrations_restic",
    edition = "2024",
    deps = [
        "@crates//:temp-env",
    ],
)
