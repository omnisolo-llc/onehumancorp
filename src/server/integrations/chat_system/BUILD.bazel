load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

rust_library(
    name = "server_integrations_chat_system",
    srcs = glob(["src/**/*.rs"]),
    visibility = ["//visibility:public"],
    deps = [
        "@crates//:serde",
        "@crates//:serde_json",
        "@crates//:tokio",
        "@crates//:uuid",
    ],
)

rust_test(
    name = "server_integrations_chat_system_test",
    crate = ":server_integrations_chat_system",
)
