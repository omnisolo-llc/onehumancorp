[package]
name = "server_common"
version = "0.1.0"
edition = "2024"

[lib]
path = "mod.rs"

[dependencies]
server_config = { path = "../config" }
serde = { version = "1.0", features = ["derive"] }
sqlx = { version = "0.8", features = ["postgres"] }
