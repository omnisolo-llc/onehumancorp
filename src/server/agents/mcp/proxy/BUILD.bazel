load("@rules_go//go:def.bzl", "go_library", "go_test")

go_library(
    name = "proxy",
    srcs = [
        "authorizer.go",
        "proxy.go",
    ],
    importpath = "github.com/onehumancorp/mono/src/server/agents/mcp/proxy",
    visibility = ["//visibility:public"],
    deps = [
        "//src/server/db",
        "//src/server/telemetry",
        "@com_github_google_uuid//:uuid",
    ],
)

go_test(
    name = "proxy_test",
    srcs = [
        "authorizer_test.go",
        "proxy_test.go",
    ],
    embed = [":proxy"],
    deps = [
        "//src/server/db",
    ],
)
