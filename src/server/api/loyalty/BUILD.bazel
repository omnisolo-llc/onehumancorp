load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "loyalty",
    srcs = glob(["**/*.rs"]),
    visibility = ["//visibility:public"],
    deps = [
        "//src/server/domain",
        "//src/server/services/loyalty",
        "//src/server/utils",
        "@crates//:axum",
        "@crates//:serde",
        "@crates//:serde_json",
        "@crates//:tokio",
    ],
)
