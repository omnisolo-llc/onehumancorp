load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "agents",
    srcs = [
        "mod.rs",
        "hire.rs",
        "approvals.rs",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Dummy deps
    ],
)
