use std::sync::Arc;
use crate::hub::Hub;

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_create_checkout_session_success() {
        assert!(true);
    }

    #[test]
    fn test_create_checkout_session_with_fx() {
        assert!(true);
    }
}
