--- src/server/api/omnichannel_webhook.rs
+++ src/server/api/omnichannel_webhook.rs
@@ -107,7 +107,7 @@ pub async fn handle_omnichannel_webhook(
                 .execute(&mut *tx)
                 .await?;

-            if payload.source == "intake_form" || payload.source == "email_inquiry" {
+            if payload.source == "intake_form" || payload.source == "email_inquiry" || payload.source == "work_intake" {
                 // Trigger Sales Agent proposal generation
                 let _job_id = sqlx::query!(
                     r#"
