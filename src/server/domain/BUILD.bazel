load("@rules_go//go:def.bzl", "go_library", "go_test")

go_library(
    name = "domain",
    srcs = [
        "mesh.go",
        "tasks.go",
    ],
    importpath = "onehumancorp/src/server/domain",
    visibility = ["//visibility:public"],
    deps = [
        "//src/proto:hub_go_proto",
    ],
)

go_test(
    name = "domain_test",
    srcs = [
        "mesh_test.go",
        "tasks_test.go",
    ],
    embed = [":domain"],
)
