load("@rules_rust//rust:defs.bzl", "rust_library", "rust_binary", "rust_test")

rust_library(
    name = "server_lib",
    srcs = glob(["**/*.rs"], exclude=["main.rs", "build.rs"]),
    visibility = ["//visibility:public"],
    deps = [
        # In a real project, we'd list external dependencies here.
        # But for this simulation, we'll keep it simple.
    ]
)

rust_binary(
    name = "server",
    srcs = ["main.rs"],
    deps = [":server_lib"],
    visibility = ["//visibility:public"],
)
