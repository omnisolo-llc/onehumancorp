load("@rules_go//go:def.bzl", "go_library", "go_test")

go_library(
    name = "kairos",
    srcs = ["mesh.go"],
    importpath = "github.com/onehumancorp/mono/src/server/orchestration/kairos",
    visibility = ["//visibility:public"],
    deps = [
        "@com_github_redis_go_redis_v9//:go-redis",
    ],
)

go_test(
    name = "kairos_test",
    srcs = ["mesh_test.go"],
    embed = [":kairos"],
    deps = [
        "@com_github_alicebob_miniredis_v2//:miniredis",
    ],
)
