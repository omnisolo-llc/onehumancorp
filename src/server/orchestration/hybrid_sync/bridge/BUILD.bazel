load("@rules_go//go:def.bzl", "go_library", "go_test")

go_library(
    name = "bridge",
    srcs = ["bridge.go"],
    importpath = "github.com/onehumancorp/hybrid_sync/bridge",
    visibility = ["//visibility:public"],
    target_compatible_with = select({
        "@platforms//os:windows": ["@platforms//:incompatible"],
        "//conditions:default": [],
    }),
)

go_test(
    name = "bridge_test",
    srcs = ["bridge_test.go"],
    embed = [":bridge"],
    deps = select({
        "@platforms//os:windows": [],
        "//conditions:default": ["@com_github_mattn_go_sqlite3//:go_default_library"],
    }),
    target_compatible_with = select({
        "@platforms//os:windows": ["@platforms//:incompatible"],
        "//conditions:default": [],
    }),
)
