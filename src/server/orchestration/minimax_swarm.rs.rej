--- src/server/orchestration/minimax_swarm.rs
+++ src/server/orchestration/minimax_swarm.rs
@@ -103,11 +103,16 @@ pub struct AgentTemplate {
     pub initial_context: Option<String>,
 }

 #[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
 pub struct AgentTurn {
+    #[serde(default)]
     pub agent_id: String,
+    #[serde(default)]
     pub role: String,
+    #[serde(default)]
     pub contribution: String,
+    #[serde(default)]
     pub handoff_to: Vec<String>,
+    #[serde(default)]
     pub confidence: f64,
 }
