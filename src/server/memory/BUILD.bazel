load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

rust_library(
    name = "memory",
    srcs = glob(["*.rs"]),
    visibility = ["//visibility:public"],
)

rust_test(
    name = "memory_test",
    crate = ":memory",
)
