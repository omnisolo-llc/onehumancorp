# Copyright 2026 Author(s) of OHC
# SPDX-License-Identifier: Apache-2.0

load("@rules_go//go:def.bzl", "go_test")

# Shared infrastructure files included in every per-category test binary.
# Splitting by category allows Bazel to cache individual test results: a change
# to agent_test.go only invalidates the e2e_agents_test binary, not the others.
_SHARED_SRCS = [
    "e2e_test.go",
    "helpers_test.go",
    "fake_llm_test.go",
]

_COMMON_DEPS = [
    "@com_github_playwright_community_playwright_go//:playwright-go",
]

_COMMON_DATA = [
    "//src/server:ohc",
    "//src/app:app",
]

_COMMON_ENV = {
    # Skip playwright host-requirements validation so tests are hermetic and
    # pass even when the host is missing optional system libraries (CI containers).
    "PLAYWRIGHT_SKIP_VALIDATE_HOST_REQUIREMENTS": "1",
}

_COMMON_TAGS = [
    "requires-network",
]

# ── Auth CUJ ──────────────────────────────────────────────────────────────────
go_test(
    name = "e2e_auth_test",
    srcs = _SHARED_SRCS + ["auth_login_test.go"],
    importpath = "github.com/onehumancorp/mono/src/tests/e2e",
    data = _COMMON_DATA,
    deps = _COMMON_DEPS,
    env = _COMMON_ENV,
    size = "large",
    timeout = "eternal",
    tags = _COMMON_TAGS,
)

go_test(
    name = "e2e_hybrid_sync_test",
    srcs = _SHARED_SRCS + [
        "hybrid_sync_e2e_test.go",
    ],
    importpath = "github.com/onehumancorp/mono/src/tests/e2e",
    data = _COMMON_DATA,
    deps = _COMMON_DEPS + [
        "@com_github_stretchr_testify//assert",
        "@com_github_stretchr_testify//require",
    ],
    env = _COMMON_ENV,
    size = "large",
    timeout = "eternal",
    tags = _COMMON_TAGS,
)

# ── Agent CUJs ────────────────────────────────────────────────────────────────
go_test(
    name = "e2e_agents_test",
    srcs = _SHARED_SRCS + [
        "agent_test.go",
        "cuj_agents_extra_test.go",
    ],
    importpath = "github.com/onehumancorp/mono/src/tests/e2e",
    data = _COMMON_DATA,
    deps = _COMMON_DEPS,
    env = _COMMON_ENV,
    size = "large",
    timeout = "eternal",
    tags = _COMMON_TAGS,
)

# ── Dashboard ─────────────────────────────────────────────────────────────────
go_test(
    name = "e2e_dashboard_test",
    srcs = _SHARED_SRCS + [
        "dashboard_test.go",
        "cuj_growth_share_test.go",
        "cuj_welcome_checklist_test.go",
    ],
    importpath = "github.com/onehumancorp/mono/src/tests/e2e",
    data = _COMMON_DATA,
    deps = _COMMON_DEPS + [
        "@com_github_stretchr_testify//assert",
        "@com_github_stretchr_testify//require",
    ],
    env = _COMMON_ENV,
    size = "large",
    timeout = "eternal",
    tags = _COMMON_TAGS,
)

# ── Budget ────────────────────────────────────────────────────────────────────
go_test(
    name = "e2e_budget_test",
    srcs = _SHARED_SRCS + [
        "budget_test.go",
        "cuj_budget_extra_test.go",
    ],
    importpath = "github.com/onehumancorp/mono/src/tests/e2e",
    data = _COMMON_DATA,
    deps = _COMMON_DEPS,
    env = _COMMON_ENV,
    size = "large",
    timeout = "eternal",
    tags = _COMMON_TAGS,
)

# ── Business ──────────────────────────────────────────────────────────────────
go_test(
    name = "e2e_business_test",
    srcs = _SHARED_SRCS + [
        "business_test.go",
        "cuj_business_extra_test.go",
    ],
    importpath = "github.com/onehumancorp/mono/src/tests/e2e",
    data = _COMMON_DATA,
    deps = _COMMON_DEPS,
    env = _COMMON_ENV,
    size = "large",
    timeout = "eternal",
    tags = _COMMON_TAGS,
)

# ── Settings ──────────────────────────────────────────────────────────────────
go_test(
    name = "e2e_settings_test",
    srcs = _SHARED_SRCS + [
        "settings_test.go",
        "cuj_settings_extra_test.go",
    ],
    importpath = "github.com/onehumancorp/mono/src/tests/e2e",
    data = _COMMON_DATA,
    deps = _COMMON_DEPS,
    env = _COMMON_ENV,
    size = "large",
    timeout = "eternal",
    tags = _COMMON_TAGS,
)

# ── Wizard ────────────────────────────────────────────────────────────────────
go_test(
    name = "e2e_wizard_test",
    srcs = _SHARED_SRCS + ["wizard_test.go"],
    importpath = "github.com/onehumancorp/mono/src/tests/e2e",
    data = _COMMON_DATA,
    deps = _COMMON_DEPS,
    env = _COMMON_ENV,
    size = "large",
    timeout = "eternal",
    tags = _COMMON_TAGS,
)

# ── Model / Provider ──────────────────────────────────────────────────────────
go_test(
    name = "e2e_model_provider_test",
    srcs = _SHARED_SRCS + ["model_provider_test.go"],
    importpath = "github.com/onehumancorp/mono/src/tests/e2e",
    data = _COMMON_DATA,
    deps = _COMMON_DEPS,
    env = _COMMON_ENV,
    size = "large",
    timeout = "eternal",
    tags = _COMMON_TAGS,
)

# ── Admin ─────────────────────────────────────────────────────────────────────
go_test(
    name = "e2e_admin_test",
    srcs = _SHARED_SRCS + ["admin_test.go"],
    importpath = "github.com/onehumancorp/mono/src/tests/e2e",
    data = _COMMON_DATA,
    deps = _COMMON_DEPS,
    env = _COMMON_ENV,
    size = "large",
    timeout = "eternal",
    tags = _COMMON_TAGS,
)

# ── CUJ Audit ─────────────────────────────────────────────────────────────────
go_test(
    name = "e2e_cuj_audit_test",
    srcs = _SHARED_SRCS + ["cuj_audit_test.go"],
    importpath = "github.com/onehumancorp/mono/src/tests/e2e",
    data = _COMMON_DATA,
    deps = _COMMON_DEPS,
    env = _COMMON_ENV,
    size = "large",
    timeout = "eternal",
    tags = _COMMON_TAGS,
)

# ── CUJ Extended ──────────────────────────────────────────────────────────────
go_test(
    name = "e2e_cuj_extended_test",
    srcs = _SHARED_SRCS + [
        "cuj_extended_test.go",
        "cuj_integration_test.go",
    ],
    importpath = "github.com/onehumancorp/mono/src/tests/e2e",
    data = _COMMON_DATA,
    deps = _COMMON_DEPS,
    env = _COMMON_ENV,
    size = "large",
    timeout = "eternal",
    tags = _COMMON_TAGS,
)

# ── CUJ Accessibility ─────────────────────────────────────────────────────────
go_test(
    name = "e2e_cuj_accessibility_test",
    srcs = _SHARED_SRCS + ["cuj_accessibility_extra_test.go"],
    importpath = "github.com/onehumancorp/mono/src/tests/e2e",
    data = _COMMON_DATA,
    deps = _COMMON_DEPS,
    env = _COMMON_ENV,
    size = "large",
    timeout = "eternal",
    tags = _COMMON_TAGS,
)

# ── Misc ──────────────────────────────────────────────────────────────────────
go_test(
    name = "e2e_misc_test",
    srcs = _SHARED_SRCS + ["misc_test.go"],
    importpath = "github.com/onehumancorp/mono/src/tests/e2e",
    data = _COMMON_DATA,
    deps = _COMMON_DEPS,
    env = _COMMON_ENV,
    size = "large",
    timeout = "eternal",
    tags = _COMMON_TAGS,
)

# ── All-in-one (backward compat alias) ───────────────────────────────────────
# Keep the original :e2e_test target as a test_suite so existing references
# (//src/tests/e2e:e2e_test) continue to work.
test_suite(
    name = "e2e_test",
    tests = [
        ":e2e_auth_test",
        ":e2e_agents_test",
        ":e2e_dashboard_test",
        ":e2e_budget_test",
        ":e2e_business_test",
        ":e2e_settings_test",
        ":e2e_hybrid_sync_test",
        ":e2e_wizard_test",
        ":e2e_model_provider_test",
        ":e2e_admin_test",
        ":e2e_cuj_audit_test",
        ":e2e_cuj_extended_test",
        ":e2e_cuj_accessibility_test",
        ":e2e_misc_test",
    ],
)
