load("@rules_flutter//flutter:defs.bzl", "dart_library", "flutter_test")

dart_library(
    name = "frontend",
    srcs = glob(["lib/**/*.dart"]),
    pubspec = "pubspec.yaml",
    visibility = ["//visibility:public"],
)

flutter_test(
    name = "tests",
    embed = [":frontend"],
    srcs = glob(["test/**/*.dart"]),
    test_files = ["test/"],
    visibility = ["//visibility:public"],
)
