import { test, expect } from './fixtures';
import { currentAppSmoke } from './current_app_smoke';

currentAppSmoke('agentic-booking');

// Removed mock tests due to testing invariants. Waiting for user input on priority frontend gap.
