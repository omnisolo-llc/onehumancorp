syntax = "proto3";

package ohc.invoicing;

option go_package = "github.com/onehumancorp/mono/src/proto/ohc/invoicing;invoicingpb";

message CreateInvoiceRequest {
  string prompt = 1;
  string customer_id = 2;
}

message InvoiceItem {
  string id = 1;
  string description = 2;
  int32 quantity = 3;
  double unit_price = 4;
}

message Invoice {
  string id = 1;
  string tenant_id = 2;
  string customer_id = 3;
  string status = 4;
  double total_amount = 5;
  repeated InvoiceItem items = 6;
  string payment_link = 7;
}

message CreateInvoiceResponse {
  Invoice invoice = 1;
}

service InvoicingService {
  rpc CreateInvoice(CreateInvoiceRequest) returns (CreateInvoiceResponse);
}
