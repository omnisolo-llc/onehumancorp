import { VerticalBox } from "std-widgets.slint"; export component GlassCard inherits Rectangle { background: rgba(255, 255, 255, 0.03); border-radius: 12px; border-width: 1px; border-color: rgba(255, 255, 255, 0.1); @children }
