import { VerticalBox } from "std-widgets.slint"; export component GlassCard inherits Rectangle { background: rgba(22, 22, 26, 0.7); border-radius: 16px; border-width: 1px; border-color: rgba(255, 255, 255, 0.1); @children }
