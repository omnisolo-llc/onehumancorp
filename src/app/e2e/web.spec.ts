/**
 * Dummy entry point for Playwright.
 * The actual tests are in the other .spec.ts files in this directory.
 */
import { test } from '@playwright/test';

test('Entry point placeholder', async () => {
  console.log('Starting Playwright test suite...');
});
