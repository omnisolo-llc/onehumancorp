--- src/app/lib/screens/pricing_screen.dart	2024-05-18 10:15:20.000000000 +0000
+++ src/app/lib/screens/pricing_screen.dart.new	2024-05-18 10:17:20.000000000 +0000
@@ -40,11 +40,15 @@
             Text(description),
             const SizedBox(height: 16),
             ElevatedButton(
-              onPressed: () {
-                if (context.canPop()) {
-                  context.pop();
-                } else {
-                  context.go('/my-plan');
-                }
+              onPressed: () async {
+                final api = ref.read(apiServiceProvider);
+                if (api != null) {
+                  try {
+                    final url = await api.createCheckoutSession(tier);
+                    ScaffoldMessenger.of(context).showSnackBar(SnackBar(content: Text('Redirecting to Stripe: \$url')));
+                  } catch (e) {
+                    ScaffoldMessenger.of(context).showSnackBar(SnackBar(content: Text('Checkout error: \$e')));
+                  }
+                }
               },
               child: const Text('Select Plan'),
