import { Button, VerticalBox, HorizontalBox } from "std-widgets.slint";

export component SwarmMemory inherits Window {
    width: 900px;
    height: 600px;
    title: "Team Memory";

    background: #f8fafc;

    HorizontalBox {
        spacing: 20px;
        padding: 24px;

        // Left Column: Live Team
        VerticalBox {
            spacing: 10px;
            width: 270px;
            
            Text { text: "Live Activity"; font-size: 20px; font-weight: 700; color: #1e293b; }
            Text { text: "Real-time updates"; color: #64748b; }
            
            Rectangle {
                background: white;
                border-color: #e2e8f0;
                border-width: 1px;
                border-radius: 8px;
                
                VerticalBox {
                    padding: 16px;
                    spacing: 10px;
                    alignment: start;
                    
                    Rectangle {
                        background: #f1f5f9;
                        border-radius: 8px;
                        
                        VerticalBox {
                            padding: 8px;
                            Text { text: "Agent Alice"; font-weight: 700; color: #a855f7; }
                            Text { text: "Processed task 123"; color: #1e293b; }
                        }
                    }
                }
            }
        }

        // Center Column: Durable Memory
        VerticalBox {
            spacing: 10px;
            width: 270px;
            
            Text { text: "Saved Memories"; font-size: 20px; font-weight: 700; color: #1e293b; }
            Text { text: "Cloud Backup"; color: #64748b; }
            
            Rectangle {
                background: white;
                border-color: #e2e8f0;
                border-width: 1px;
                border-radius: 8px;
                
                VerticalBox {
                    padding: 16px;
                    spacing: 10px;
                    alignment: start;
                    
                    Rectangle {
                        background: #f1f5f9;
                        border-radius: 8px;
                        
                        VerticalBox {
                            padding: 8px;
                            Text { text: "Project goal defined"; color: #1e293b; }
                            Text { text: "2026-04-25 12:00"; color: #64748b; font-size: 12px; }
                        }
                    }
                }
            }
        }

        // Right Column: AutoDream
        VerticalBox {
            spacing: 10px;
            width: 270px;
            
            Text { text: "Automated Goals"; font-size: 20px; font-weight: 700; color: #1e293b; }
            Text { text: "Memory Overview"; color: #64748b; }
            
            Rectangle {
                background: white;
                border-color: #e2e8f0;
                border-width: 1px;
                border-radius: 8px;
                
                VerticalBox {
                    padding: 16px;
                    spacing: 10px;
                    Text { text: "[ Team Speed Chart ]"; color: #64748b; horizontal-alignment: center; }
                    Text { text: "[ Memory Overview ]"; color: #64748b; horizontal-alignment: center; }
                    Button { text: "View Walkthrough"; }
                }
            }
        }
    }
}
