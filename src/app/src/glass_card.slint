export component GlassCard inherits Rectangle {
    background: rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    border-width: 1px;
    border-color: rgba(255, 255, 255, 0.2);
    // Note: Slint might not fully support CSS backdrop-filter, but we can simulate it or rely on the host platform
    // drop-shadow-blur: 20px;
    // drop-shadow-color: rgba(0, 0, 0, 0.2);
}
