import { Button, VerticalBox, HorizontalBox } from "std-widgets.slint";

export component Pipelines inherits Window {
    width: 500px;
    height: 600px;
    title: "Business Workflows";

    background: #f8fafc;

    VerticalBox {
        spacing: 20px;
        padding: 24px;

        HorizontalBox {
            Text { text: "Business Workflows"; font-size: 20px; font-weight: 700; color: #1e293b; }
            Rectangle {}
            Button { text: "Refresh"; }
        }

        VerticalBox {
            spacing: 16px;
            
            Rectangle {
                background: white;
                border-color: #e2e8f0;
                border-width: 1px;
                border-radius: 8px;
                
                VerticalBox {
                    padding: 16px;
                    spacing: 12px;
                    
                    HorizontalBox {
                        VerticalBox {
                            Text { text: "Main Workflow"; font-weight: 700; color: #1e293b; }
                            Text { text: "version: current"; color: #64748b; }
                        }
                        Rectangle {}
                        Text { text: "DRAFT"; color: #06b6d4; horizontal-alignment: center; }
                    }
                    
                    Text { text: "Initiated by: Business Automation"; color: #64748b; }
                    
                    Rectangle {
                        background: #e2e8f0;
                        border-radius: 4px;
                        
                        HorizontalBox {
                            padding: 8px;
                            Text { text: "http://staging.example.com"; color: #0369a1; } 
                        }
                    }
                    
                    Text { text: "Publish to Live"; color: #1e293b; font-weight: 700; }
                    
                    Button {
                        text: "Slide to Approve (Tap)";
                    }
                }
            }
        }
    }
}
