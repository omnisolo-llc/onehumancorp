import { Button, VerticalBox, HorizontalBox, LineEdit, CheckBox } from "std-widgets.slint";

export component Wizard inherits Window {
    width: 375px;
    height: 667px;
    title: "Setup Wizard";

    property <int> step: 0;
    property <bool> is_advanced: false;

    background: #f8fafc;

    VerticalBox {
        spacing: 20px;
        padding: 24px;

        HorizontalBox {
            Text { text: "Setup Wizard"; font-size: 24px; font-weight: 700; color: #1e293b; }
            Rectangle {}
            CheckBox {
                text: "Expert Mode";
                checked: is_advanced;
                toggled => { is_advanced = !is_advanced; }
            }
        }

        HorizontalBox {
            spacing: 10px;
            alignment: center;
            
            Rectangle { 
                background: step >= 0 ? #a855f7 : #e2e8f0; 
                border-radius: 12px; 
                width: 24px; 
                height: 24px; 
                HorizontalBox {
                    padding: 2px;
                    Text { text: "1"; color: white; horizontal-alignment: center; } 
                }
            }
            Text { text: "Server"; color: step == 0 ? #1e293b : #64748b; }
            
            Rectangle { 
                background: step >= 1 ? #a855f7 : #e2e8f0; 
                border-radius: 12px; 
                width: 24px; 
                height: 24px; 
                HorizontalBox {
                    padding: 2px;
                    Text { text: "2"; color: white; horizontal-alignment: center; } 
                }
            }
            Text { text: "AI Provider"; color: step == 1 ? #1e293b : #64748b; }
            
            Rectangle { 
                background: step >= 2 ? #a855f7 : #e2e8f0; 
                border-radius: 12px; 
                width: 24px; 
                height: 24px; 
                HorizontalBox {
                    padding: 2px;
                    Text { text: "3"; color: white; horizontal-alignment: center; } 
                }
            }
            Text { text: "Real-time"; color: step == 2 ? #1e293b : #64748b; }
        }

        if (step == 0) : VerticalBox {
            spacing: 15px;
            Text { text: "Server Settings"; font-size: 20px; font-weight: 700; color: #1e293b; }
            LineEdit { placeholder-text: "Listen Address"; text: "0.0.0.0:18789"; }
            if (is_advanced) : LineEdit { placeholder-text: "Database Path"; text: "ohc.db"; }
        }

        if (step == 1) : VerticalBox {
            spacing: 15px;
            Text { text: "AI Provider — Minimax"; font-size: 20px; font-weight: 700; color: #1e293b; }
            LineEdit { placeholder-text: "Minimax API Key"; }
            if (is_advanced) : LineEdit { placeholder-text: "Default Model"; text: "abab6.5s"; }
        }

        if (step == 2) : VerticalBox {
            spacing: 15px;
            Text { text: "Real-time Messaging (Centrifuge)"; font-size: 20px; font-weight: 700; color: #1e293b; wrap: word-wrap; }
            LineEdit { placeholder-text: "Centrifuge WebSocket URL"; text: "ws://localhost:8000/connection/websocket"; }
            if (is_advanced) : LineEdit { placeholder-text: "Centrifuge HMAC Secret"; }
        }

        Rectangle {} 

        HorizontalBox {
            if (step > 0) : Button {
                text: "Back";
                clicked => { step = step - 1; }
            }
            Rectangle {} 
            Button {
                text: step == 2 ? "Save Configuration" : "Next";
                clicked => {
                    if (step < 2) {
                        step = step + 1;
                    } else {
                        // Handle save
                    }
                }
            }
        }
    }
}
