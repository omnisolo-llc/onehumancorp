import { Button, VerticalBox, HorizontalBox, ScrollView } from "std-widgets.slint";

export component ReleaseNotes inherits Window {
    width: 100%;
    height: 100%;
    title: "What's New";
    background: #f8fafc;

    VerticalBox {
        padding: 16px;
        spacing: 20px;

        Text {
            text: "What's New in OHC";
            font-size: 24px;
            font-weight: 700;
            color: #0f172a;
        }

        ScrollView {
            VerticalBox {
                spacing: 24px;

                VerticalBox {
                    spacing: 8px;
                    Text { text: "Version 1.2.0 - October 2023"; font-weight: 700; font-size: 14px; color: #64748b; }
                    Rectangle {
                        background: white; border-radius: 8px;
                        VerticalBox {
                            padding: 16px; spacing: 12px;
                            Text { text: "🎉 New Help Center"; font-weight: 700; font-size: 18px; color: #0f172a; }
                            Text { text: "We've added a brand new Help Center right inside the app. Now you can find answers to your questions without ever leaving your store dashboard."; color: #475569; wrap: word-wrap; }
                        }
                    }
                }

                VerticalBox {
                    spacing: 8px;
                    Text { text: "Version 1.1.5 - September 2023"; font-weight: 700; font-size: 14px; color: #64748b; }
                    Rectangle {
                        background: white; border-radius: 8px;
                        VerticalBox {
                            padding: 16px; spacing: 12px;
                            Text { text: "🚀 Faster Agent Responses"; font-weight: 700; font-size: 18px; color: #0f172a; }
                            Text { text: "Your AI agents are now 2x faster at replying to your customers on Instagram and email."; color: #475569; wrap: word-wrap; }
                        }
                    }
                }
            }
        }
    }
}
