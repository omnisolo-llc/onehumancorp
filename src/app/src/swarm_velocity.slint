export component SwarmVelocity inherits Rectangle {
    in-out property <string> completion_rate: "0%";
    in-out property <string> avg_latency: "0ms";
    in-out property <int> active_threads: 0;
}
