import { Button, VerticalBox, HorizontalBox } from "std-widgets.slint";

export component MyPlan inherits Window {
    width: 375px;
    height: 400px;
    title: "My Plan";

    background: #f8fafc;

    VerticalBox {
        spacing: 20px;
        padding: 24px;

        Text { text: "My Plan"; font-size: 20px; font-weight: 700; color: #1e293b; }

        Rectangle {
            background: white;
            border-color: #e2e8f0;
            border-width: 1px;
            border-radius: 8px;
            
            VerticalBox {
                padding: 16px;
                spacing: 8px;
                Text { text: "Current Plan"; color: #64748b; }
                Text { text: "Pro Tier"; font-size: 28px; font-weight: 700; color: #1e293b; }
                Button { text: "Upgrade"; }
            }
        }

        Rectangle {
            background: white;
            border-color: #e2e8f0;
            border-width: 1px;
            border-radius: 8px;
            
            VerticalBox {
                padding: 16px;
                spacing: 8px;
                Text { text: "Usage This Month"; font-size: 18px; font-weight: 700; color: #1e293b; }
                Text { text: "AI Actions: 1200 / Unlimited"; color: #1e293b; }
                Text { text: "Storage Used: 150.5 MB / 10.0 GB"; color: #1e293b; }
                Text { text: "Estimated Next Bill: $124.50"; color: #1e293b; }
            }
        }
    }
}
