import { BaseWindow, VerticalBox } from "shim.slint";

export component SwarmVelocity {
    in-out property <string> active_threads;
    in-out property <string> avg_latency;
    in-out property <string> completion_rate;
    VerticalBox {
    }
}
