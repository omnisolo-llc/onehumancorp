import { Button, VerticalBox, HorizontalBox, ScrollView } from "std-widgets.slint";

export struct UiMcpTool {
    id: string,
    name: string,
    description: string,
}

export component Integrations inherits Window {
    width: 500px;
    height: 600px;
    title: "Integrations & Tools ";
    out property <string> test_title: root.title;

    background: #f8fafc;

    in property <[UiMcpTool]> tools;
    callback configure_integration(string);
    callback invoke_tool(string);

    ScrollView {
        VerticalBox {
            spacing: 20px;
            padding: 24px;

        Text { text: "External Channels"; font-size: 20px; font-weight: 700; color: #1e293b; }

        HorizontalBox {
            spacing: 16px;

            Rectangle {
                background: white;
                border-color: #e2e8f0;
                border-width: 1px;
                border-radius: 8px;
                width: 220px;

                VerticalBox {
                    padding: 16px;
                    spacing: 8px;
                    Text { text: "📘 Facebook"; font-weight: 700; color: #1e293b; }
                    Text { text: "Unified Social Media DMs"; color: #64748b; wrap: word-wrap; }
                    Button {
                        text: "Configure";
                        clicked => { configure_integration("Facebook"); }
                    }
                }
            }

            Rectangle {
                background: white;
                border-color: #e2e8f0;
                border-width: 1px;
                border-radius: 8px;
                width: 220px;

                VerticalBox {
                    padding: 16px;
                    spacing: 8px;
                    Text { text: "📷 Instagram"; font-weight: 700; color: #1e293b; }
                    Text { text: "Unified Social Media DMs"; color: #64748b; wrap: word-wrap; }
                    Button {
                        text: "Configure";
                        clicked => { configure_integration("Instagram"); }
                    }
                }
            }
        }

        HorizontalBox {
            spacing: 16px;

            Rectangle {
                background: white;
                border-color: #e2e8f0;
                border-width: 1px;
                border-radius: 8px;
                width: 220px;

                VerticalBox {
                    padding: 16px;
                    spacing: 8px;
                    Text { text: "💬 WhatsApp"; font-weight: 700; color: #1e293b; }
                    Text { text: "Unified Social Media DMs"; color: #64748b; wrap: word-wrap; }
                    Button {
                        text: "Configure";
                        clicked => { configure_integration("WhatsApp"); }
                    }
                }
            }

            Rectangle {
                background: white;
                border-color: #e2e8f0;
                border-width: 1px;
                border-radius: 8px;
                width: 220px;

                VerticalBox {
                    padding: 16px;
                    spacing: 8px;
                    Text { text: "✈️ Telegram"; font-weight: 700; color: #1e293b; }
                    Text { text: "Connect your bot for mobile alerts"; color: #64748b; wrap: word-wrap; }
                    Button {
                        text: "Configure";
                        clicked => { configure_integration("Telegram"); }
                    }
                }
            }
        }

        HorizontalBox {
            spacing: 16px;

            Rectangle {
                background: white;
                border-color: #e2e8f0;
                border-width: 1px;
                border-radius: 8px;
                width: 220px;

                VerticalBox {
                    padding: 16px;
                    spacing: 8px;
                    Text { text: "👾 Discord"; font-weight: 700; color: #1e293b; }
                    Text { text: "Stream agent logs to a channel"; color: #64748b; wrap: word-wrap; }
                    Button {
                        text: "Configure";
                        clicked => { configure_integration("Discord"); }
                    }
                }
            }

            Rectangle {
                background: white;
                border-color: #e2e8f0;
                border-width: 1px;
                border-radius: 8px;
                width: 220px;

                VerticalBox {
                    padding: 16px;
                    spacing: 8px;
                    Text { text: "💬 Twilio"; font-weight: 700; color: #1e293b; }
                    Text { text: "Send SMS alerts to customers"; color: #64748b; wrap: word-wrap; }
                    Button {
                        text: "Configure";
                        clicked => { configure_integration("Twilio"); }
                    }
                }
            }
        }

        HorizontalBox {
            spacing: 16px;

            Rectangle {
                background: white;
                border-color: #e2e8f0;
                border-width: 1px;
                border-radius: 8px;
                width: 220px;

                VerticalBox {
                    padding: 16px;
                    spacing: 8px;
                    Text { text: "💬 Twilio"; font-weight: 700; color: #1e293b; }
                    Text { text: "Send SMS alerts to customers"; color: #64748b; wrap: word-wrap; }
                    Button {
                        text: "Configure";
                        clicked => { configure_integration("Twilio"); }
                    }
                }
            }

            Rectangle {
                background: white;
                border-color: #e2e8f0;
                border-width: 1px;
                border-radius: 8px;
                width: 220px;

                VerticalBox {
                    padding: 16px;
                    spacing: 8px;
                    Text { text: "📅 Cal.com"; font-weight: 700; color: #1e293b; }
                    Text { text: "Automated appointment booking"; color: #64748b; wrap: word-wrap; }
                    Button {
                        text: "Configure";
                        clicked => { configure_integration("Cal.com"); }
                    }
                }
            }
        }

        HorizontalBox {
            spacing: 16px;

            Rectangle {
                background: white;
                border-color: #e2e8f0;
                border-width: 1px;
                border-radius: 8px;
                width: 220px;

                VerticalBox {
                    padding: 16px;
                    spacing: 8px;
                    Text { text: "📅 Calendly"; font-weight: 700; color: #1e293b; }
                    Text { text: "Automated Booking"; color: #64748b; wrap: word-wrap; }
                    Button {
                        text: "Configure";
                        clicked => { configure_integration("Calendly"); }
                    }
                }
            }

            Rectangle {
                background: white;
                border-color: #e2e8f0;
                border-width: 1px;
                border-radius: 8px;
                width: 220px;

                VerticalBox {
                    padding: 16px;
                    spacing: 8px;
                    Text { text: "💬 Chatwoot"; font-weight: 700; color: #1e293b; }
                    Text { text: "Unified Social Media DMs"; color: #64748b; wrap: word-wrap; }
                    Button {
                        text: "Configure";
                        clicked => { configure_integration("Chatwoot"); }
                    }
                }
            }

            Rectangle {
                background: white;
                border-color: #e2e8f0;
                border-width: 1px;
                border-radius: 8px;
                width: 220px;

                VerticalBox {
                    padding: 16px;
                    spacing: 8px;
                    Text { text: "💬 Ayrshare"; font-weight: 700; color: #1e293b; }
                    Text { text: "Unified Social Media Inbox & Cross-Posting"; color: #64748b; wrap: word-wrap; }
                    Button {
                        text: "Configure";
                        clicked => { configure_integration("Ayrshare"); }
                    }
                }
            }
        }


        HorizontalBox {
            spacing: 16px;

            Rectangle {
                background: white;
                border-color: #e2e8f0;
                border-width: 1px;
                border-radius: 8px;
                width: 220px;

                VerticalBox {
                    padding: 16px;
                    spacing: 8px;
                    Text { text: "💬 Meta"; font-weight: 700; color: #1e293b; }
                    Text { text: "Unified Social Media Inbox"; color: #64748b; wrap: word-wrap; }
                    Button {
                        text: "Configure";
                        clicked => { configure_integration("Meta"); }
                    }
                }
            }

            Rectangle {
                background: white;
                border-color: #e2e8f0;
                border-width: 1px;
                border-radius: 8px;
                width: 220px;

                VerticalBox {
                    padding: 16px;
                    spacing: 8px;
                    Text { text: "📧 Resend"; font-weight: 700; color: #1e293b; }
                    Text { text: "Native Email Campaign Manager"; color: #64748b; wrap: word-wrap; }
                    Button {
                        text: "Configure";
                        clicked => { configure_integration("Resend"); }
                    }
                }
            }

            Rectangle {
                background: white;
                border-color: #e2e8f0;
                border-width: 1px;
                border-radius: 8px;
                width: 220px;

                VerticalBox {
                    padding: 16px;
                    spacing: 8px;
                    Text { text: "📧 Listmonk"; font-weight: 700; color: #1e293b; }
                    Text { text: "Embedded No-Jargon Email Campaigns"; color: #64748b; wrap: word-wrap; }
                    Button {
                        text: "Configure";
                        clicked => { configure_integration("Listmonk"); }
                    }
                }
            }
        }

        HorizontalBox {
            spacing: 16px;

            Rectangle {
                background: white;
                border-color: #e2e8f0;
                border-width: 1px;
                border-radius: 8px;
                width: 220px;

                VerticalBox {
                    padding: 16px;
                    spacing: 8px;
                    Text { text: "📧 Mailchimp"; font-weight: 700; color: #1e293b; }
                    Text { text: "Customer Re-engagement"; color: #64748b; wrap: word-wrap; }
                    Button {
                        text: "Configure";
                        clicked => { configure_integration("Mailchimp"); }
                    }
                }
            }

            Rectangle {
                background: white;
                border-color: #e2e8f0;
                border-width: 1px;
                border-radius: 8px;
                width: 220px;

                VerticalBox {
                    padding: 16px;
                    spacing: 8px;
                    Text { text: "📦 Shippo"; font-weight: 700; color: #1e293b; }
                    Text { text: "Shipping Rate & Label Generation"; color: #64748b; wrap: word-wrap; }
                    Button {
                        text: "Configure";
                        clicked => { configure_integration("Shippo"); }
                    }
                }
            }

            Rectangle {
                background: white;
                border-color: #e2e8f0;
                border-width: 1px;
                border-radius: 8px;
                width: 220px;

                VerticalBox {
                    padding: 16px;
                    spacing: 8px;
                    Text { text: "📦 EasyPost"; font-weight: 700; color: #1e293b; }
                    Text { text: "Painless Shipping Labels & Tracking"; color: #64748b; wrap: word-wrap; }
                    Button {
                        text: "Configure";
                        clicked => { configure_integration("EasyPost"); }
                    }
                }
            }
        }

        HorizontalBox {
            spacing: 16px;

            Rectangle {
                background: white;
                border-color: #e2e8f0;
                border-width: 1px;
                border-radius: 8px;
                width: 220px;

                VerticalBox {
                    padding: 16px;
                    spacing: 8px;
                    Text { text: "💳 Mercado Pago"; font-weight: 700; color: #1e293b; }
                    Text { text: "Local Payment Methods (LATAM)"; color: #64748b; wrap: word-wrap; }
                    Button {
                        text: "Configure";
                        clicked => { configure_integration("Mercado Pago"); }
                    }
                }
            }

            Rectangle {
                background: white;
                border-color: #e2e8f0;
                border-width: 1px;
                border-radius: 8px;
                width: 220px;

                VerticalBox {
                    padding: 16px;
                    spacing: 8px;
                    Text { text: "💳 Razorpay"; font-weight: 700; color: #1e293b; }
                    Text { text: "Local Payment Methods (India)"; color: #64748b; wrap: word-wrap; }
                    Button {
                        text: "Configure";
                        clicked => { configure_integration("Razorpay"); }
                    }
                }
            }
        }

        HorizontalBox {
            spacing: 16px;

            Rectangle {
                background: white;
                border-color: #e2e8f0;
                border-width: 1px;
                border-radius: 8px;
                width: 220px;

                VerticalBox {
                    padding: 16px;
                    spacing: 8px;
                    Text { text: "📹 Zoom"; font-weight: 700; color: #1e293b; }
                    Text { text: "Native Link Generation"; color: #64748b; wrap: word-wrap; }
                    Button {
                        text: "Configure";
                        clicked => { configure_integration("Zoom"); }
                    }
                }
            }

            Rectangle {
                background: white;
                border-color: #e2e8f0;
                border-width: 1px;
                border-radius: 8px;
                width: 220px;

                VerticalBox {
                    padding: 16px;
                    spacing: 8px;
                    Text { text: "📹 Jitsi Meet"; font-weight: 700; color: #1e293b; }
                    Text { text: "Zero-Setup Online Lessons"; color: #64748b; wrap: word-wrap; }
                    Button {
                        text: "Configure";
                        clicked => { configure_integration("Jitsi"); }
                    }
                }
            }
        }

        Text { text: "Connect Custom Software"; font-size: 20px; font-weight: 700; color: #1e293b; }
        Text { text: "Manually trigger advanced connected tools for your business."; color: #64748b; }

        Rectangle {
            background: white;
            border-color: #e2e8f0;
            border-width: 1px;
            border-radius: 8px;

            VerticalBox {
                padding: 16px;
                spacing: 10px;

                for tool in tools : HorizontalBox {
                    Text { text: "🛠️ " + tool.name; font-weight: 700; color: #1e293b; horizontal-alignment: center; }
                    Rectangle {}
                    Button {
                        text: "Invoke";
                        clicked => { invoke_tool(tool.id); }
                    }
                }
            }
          }
    }
    }
}
