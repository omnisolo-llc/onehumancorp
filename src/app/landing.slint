import { Button, VerticalBox, HorizontalBox } from "std-widgets.slint";
import { GlassCard } from "glass_card.slint";

export component Landing inherits Window {
    width: 800px;
    height: 600px;
    title: "OneHumanCorp";
    out property <string> test_title: root.title;

    in-out property <bool> is_variant_b: false;

    callback start_business_setup();
    callback continue_to_dashboard();
    callback download(string);

    background: @linear-gradient(135deg, #0D0D1A 0%, #1A1A33 100%);

    VerticalBox {
        spacing: 20px;
        padding: 32px;
        alignment: center;

        // Header
        Text { text: "OneHumanCorp"; font-size: 32px; font-weight: 700; color: white; horizontal-alignment: center; }
        
        Text { 
            text: is_variant_b ? "The Cloud Helper System" : "The Flexible Helper System";
            font-size: 24px; 
            color: #a855f7; 
            horizontal-alignment: center; 
        }

        Text {
            text: is_variant_b ? 
                "Grow your business with seamless cloud convenience. Collaborate instantly with your team from anywhere in the world." :
                "Grow your business while keeping your ideas safe. Experience the gold standard for privacy with our local desktop app.";
            color: #64748b;
            horizontal-alignment: center;
            wrap: word-wrap;
        }

        // Value Props
        HorizontalBox {
            spacing: 20px;
            alignment: center;
            
            if (is_variant_b) : GlassCard {
                width: 220px; height: 150px;
                VerticalBox {
                    padding: 16px;
                    Text { text: "Always Online"; font-weight: 700; color: white; }
                    Text { text: "Your helpers are always awake, replying to customers and taking orders while you sleep."; color: #94a3b8; wrap: word-wrap; }
                }
            }
            if (is_variant_b) : GlassCard {
                width: 220px; height: 150px;
                VerticalBox {
                    padding: 16px;
                    Text { text: "Instant Collaboration"; font-weight: 700; color: white; }
                    Text { text: "Invite team members instantly. Shared intelligence across your entire organization."; color: #94a3b8; wrap: word-wrap; }
                }
            }
            
            if (!is_variant_b) : GlassCard {
                width: 220px; height: 150px;
                VerticalBox {
                    padding: 16px;
                    Text { text: "Complete Privacy"; font-weight: 700; color: white; }
                    Text { text: "All helpers run entirely on your computer. Your business data never leaves your device."; color: #94a3b8; wrap: word-wrap; }
                }
            }
            if (!is_variant_b) : GlassCard {
                width: 220px; height: 150px;
                VerticalBox {
                    padding: 16px;
                    Text { text: "Works Offline"; font-weight: 700; color: white; }
                    Text { text: "No internet? No problem. Continue managing your business without a connection."; color: #94a3b8; wrap: word-wrap; }
                }
            }
        }

        // Downloads
        HorizontalBox {
            spacing: 10px;
            alignment: center;
            Button { text: "Download for Mac"; clicked => { download("Mac"); } }
            Button { text: "Download for Windows"; clicked => { download("Windows"); } }
            Button { text: "Download for Linux"; clicked => { download("Linux"); } }
        }

        // Navigation
        Button { text: "Start Business Setup"; clicked => { start_business_setup(); } }
        Button { text: "Or continue to Cloud Dashboard"; clicked => { continue_to_dashboard(); } }

        // Debug toggle
        HorizontalBox {
            alignment: center;
            Text { text: "Debug: Switch Variant"; color: white; }
            Button { text: "Toggle"; clicked => { is_variant_b = !is_variant_b; } }
        }
    }
}
