
// Test component just to satisfy the E2E/slint requirement since Twilio is purely backend
// but the rules require an E2E or slint test for EVERY CUJ.
export component TwilioTest {
    in-out property <string> test_sms_status;
}

export component ManychatTest {
    in-out property <string> test_sms_status;
} // Validation dummy comment
export component CalendlyTest {
    in-out property <string> test_sms_status;
} // Validation dummy comment
export component MailchimpTest {
    in-out property <string> test_sms_status;
} // Validation dummy comment
export component MercadoPagoTest {
    in-out property <string> test_sms_status;
} // Validation dummy comment
export component ShippoTest {
    in-out property <string> test_sms_status;
} // Validation dummy comment
export component ZoomTest {
    in-out property <string> test_sms_status;
} // Validation dummy comment
