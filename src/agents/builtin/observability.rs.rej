--- src/agents/builtin/observability.rs
+++ src/agents/builtin/observability.rs
@@ -14,8 +14,8 @@

 #[derive(Debug, Clone)]
 pub struct LangSmithProvider {
-    api_key: String,
-    project_name: String,
+    _api_key: String,
+    _project_name: String,
 }

 impl LangSmithProvider {
@@ -23,8 +23,8 @@
         let api_key = std::env::var("LANGSMITH_API_KEY").unwrap_or_default();
         let project_name = std::env::var("LANGSMITH_PROJECT").unwrap_or_else(|_| "default".to_string());
         Self {
-            api_key,
-            project_name,
+            _api_key: api_key,
+            _project_name: project_name,
         }
     }
 }
@@ -49,15 +49,15 @@

 #[derive(Debug, Clone)]
 pub struct LangfuseProvider {
-    public_key: String,
-    secret_key: String,
+    _public_key: String,
+    _secret_key: String,
 }

 impl LangfuseProvider {
     pub fn new() -> Self {
         Self {
-            public_key: std::env::var("LANGFUSE_PUBLIC_KEY").unwrap_or_default(),
-            secret_key: std::env::var("LANGFUSE_SECRET_KEY").unwrap_or_default(),
+            _public_key: std::env::var("LANGFUSE_PUBLIC_KEY").unwrap_or_default(),
+            _secret_key: std::env::var("LANGFUSE_SECRET_KEY").unwrap_or_default(),
         }
     }
 }
