--- agent.rs
+++ agent.rs
@@ -1318,7 +1318,7 @@
                     // If it fails, wait and try again
                     match parse_structured_output::<T>(&parser_client, req.clone(), 3).await {
                         Ok(res) => return Ok(res),
                         Err(e) => {
-                            let _retry_count = 0;
+                            let mut retry_count = 0;
                             let max_retries = std::cmp::min(cfg_max_retries, 2); // Error Handling (Compounding Error Prevention): Stripe limits retries to exactly 2.

--- /dev/null
+++ /dev/null
@@ -1426,7 +1426,7 @@
             let mut retry_count = 0;
             let max_retries = std::cmp::min(cfg_max_retries, 2); // Error Handling (Compounding Error Prevention): Stripe limits retries to exactly 2.
             loop {
-            let _retry_count = 0;
+            let mut retry_count = 0;
                 let parser_client = Arc::new(ParserAdapter { llm: self.llm.clone() }) as Arc<dyn crate::output_parser::LlmClientForParser>;

                 // We don't bubble up here because we want to feed it back into the normal TAO loop as a failure
