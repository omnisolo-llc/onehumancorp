--- src/agents/builtin/tools/checkout.rs
+++ src/agents/builtin/tools/checkout.rs
@@ -26,7 +26,7 @@
     pub async fn generate_payment_link(&self, item_name: &str, amount_cents: i64) -> Result<String, String> {
         // Generate an ad-hoc payment link via Stripe proxy
-        let amount_usd = amount_cents as f64 / 100.0;
+        let _amount_usd = amount_cents as f64 / 100.0;
         tracing::info!("Generated payment link for {} at {} cents", item_name, amount_cents);
         Ok(format!("https://checkout.onehumancorp.com/pay/{}", uuid::Uuid::new_v4()))
     }
--- src/server/workers/department_workers.rs
+++ src/server/workers/department_workers.rs
@@ -809,7 +809,7 @@
         });

-        let db = self.db.clone();
+        let _db = self.db.clone();
         tokio::spawn(async move {
             let mut interval = tokio::time::interval(std::time::Duration::from_secs(3600)); // Hourly
             loop {
