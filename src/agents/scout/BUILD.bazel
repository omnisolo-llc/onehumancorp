load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

rust_library(
    name = "scout_lib",
    srcs = [
        "agent.rs",
        "db.rs",
        "lib.rs",
    ],
    edition = "2024",
    visibility = ["//visibility:public"],
    deps = [
        "//src/agents/builtin:core",
        "@crates//:chrono",
        "@crates//:serde",
        "@crates//:serde_json",
        "@crates//:sqlx",
        "@crates//:tokio",
        "@crates//:tracing",
        "@crates//:uuid",
    ],
)

rust_test(
    name = "scout_unit_test",
    size = "small",
    srcs = ["tests.rs"],
    edition = "2024",
    deps = [
        ":scout_lib",
        "//src/agents/builtin:core",
        "@crates//:tokio",
        "@crates//:sqlx",
        "@crates//:serde_json",
        "@crates//:chrono",
        "@crates//:uuid",
    ],
    target_compatible_with = select({
        "@platforms//os:linux": [],
        "//conditions:default": ["@platforms//:incompatible"],
    }),
)
