load("@rules_rust//rust:defs.bzl", "rust_test")

rust_test(
    name = "builtin_agent_test",
    size = "small",
    srcs = ["//src/agents/builtin:agent_test.rs"],
    edition = "2024",
    target_compatible_with = select({
        "@platforms//os:linux": [],
        "//conditions:default": ["@platforms//:incompatible"],
    }),
)
