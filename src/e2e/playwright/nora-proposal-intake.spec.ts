import { test, expect } from '../fixtures';

test.describe('Nora Autonomous Proposal Intake Flow', () => {
  test('Client intake loads', async ({ page }) => {
    // Check Client View
    await page.goto(`/`);
  });
});
