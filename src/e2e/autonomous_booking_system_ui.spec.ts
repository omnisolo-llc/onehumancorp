import { test, expect } from '@playwright/test';

test.describe('Autonomous Booking System UI CUJ', () => {
  test('Customer clicks a time and books', async ({ page }) => {
    expect(true).toBe(true);
  });
});
