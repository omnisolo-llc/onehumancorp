import { test, expect } from '@playwright/test';
import { adminPage } from './fixtures';

test.describe('Triage Actions execution tests', () => {
    test('dummy test', async ({ page }) => {
        expect(true).toBeTruthy();
    });
});
