apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: agent-mtls-enforcement
  namespace: onehumancorp
spec:
  action: ALLOW
  rules:
  - from:
    - source:
        principals: ["cluster.local/ns/onehumancorp/sa/*"]
