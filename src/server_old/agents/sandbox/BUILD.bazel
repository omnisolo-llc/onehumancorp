load("@rules_go//go:def.bzl", "go_library", "go_test")

go_library(
    name = "sandbox",
    srcs = [
        "sandbox.go",
        "session.go",
    ],
    importpath = "github.com/onehumancorp/mono/src/server_old/agents/sandbox",
    visibility = ["//visibility:public"],
)

go_test(
    name = "sandbox_test",
    srcs = [
        "sandbox_test.go",
        "session_test.go",
    ],
    embed = [":sandbox"],
)
